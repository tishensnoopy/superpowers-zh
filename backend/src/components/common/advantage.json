{
  "kind": "component",
  "collectionName": "components_common_advantages",
  "info": {
    "singularName": "advantage",
    "pluralName": "advantages",
    "displayName": "Advantage",
    "description": "An advantage item",
    "icon": "Star"
  },
  "attributes": {
    "title": {
      "type": "string",
      "required": true,
      "maxLength": 200,
      "description": "优势标题",
      "pluginOptions": {
        "i18n": {
          "localized": true
        }
      }
    },
    "description": {
      "type": "string",
      "maxLength": 500,
      "description": "优势描述",
      "pluginOptions": {
        "i18n": {
          "localized": true
        }
      }
    },
    "icon": {
      "type": "string",
      "maxLength": 100,
      "description": "图标名称（来自图标库，如 Star / Award / Shield）",
      "pluginOptions": {
        "i18n": {
          "localized": true
        }
      }
    },
    "color": {
      "type": "string",
      "maxLength": 20,
      "description": "图标颜色，HEX 格式（如 #F5851F），留空使用默认色 #F5851F",
      "default": "#F5851F",
      "pluginOptions": {
        "i18n": {
          "localized": true
        }
      }
    },
    "bgColor": {
      "type": "string",
      "maxLength": 20,
      "description": "图标背景色，HEX 格式（如 #FFF3E5），留空使用默认色 #FFF3E5",
      "default": "#FFF3E5",
      "pluginOptions": {
        "i18n": {
          "localized": true
        }
      }
    }
  }
}
