{
  "collectionName": "components_common_analytics",
  "info": {
    "displayName": "统计代码",
    "description": "第三方分析统计代码配置，留空则不加载对应脚本",
    "icon": "chart"
  },
  "options": {},
  "attributes": {
    "ga4Id": {
      "type": "string",
      "maxLength": 50,
      "description": "Google Analytics 4 衡量 ID（如 G-XXXXXXXXXX），留空不加载"
    },
    "baiduTongjiId": {
      "type": "string",
      "maxLength": 50,
      "description": "百度统计 ID（如 a1b2c3d4e5f6...），留空不加载"
    },
    "facebookPixelId": {
      "type": "string",
      "maxLength": 50,
      "description": "Facebook Pixel ID（纯数字），留空不加载"
    }
  }
}
