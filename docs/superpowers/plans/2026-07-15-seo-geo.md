# 5B-1 前端 SEO 基础 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 扩展 JSON-LD 结构化数据覆盖（Organization、LocalBusiness、BreadcrumbList、Person、增强 Course/NewsArticle/WebSite/FAQPage），补全 sitemap 动态 URL，移除遗留客户端 SEO 组件。

**架构：** 在 `lib/seo.ts` 新增 8 个 schema 生成器函数，返回 `Record<string, unknown>`，由现有 `buildJsonLd()` 序列化。全局 schema（Organization + WebSite）在 `layout.tsx` 注入，页面级 schema 在各页面注入。Sitemap 补全 campuses/teachers 动态 URL。

**技术栈：** Next.js App Router、TypeScript、vitest（单元测试）、Playwright（E2E 测试）

**规格文件：** `docs/superpowers/specs/2026-07-15-seo-geo-design.md`

---

## 文件结构

**修改的文件：**
- `frontend-next/lib/seo.ts` — 新增 8 个 schema 生成器函数（现有 69 行 → ~300 行）
- `frontend-next/lib/__tests__/seo.test.ts` — 新增 8 个生成器的单元测试（现有 41 行 → ~400 行）
- `frontend-next/app/[locale]/layout.tsx` — 注入 Organization + WebSite 全局 schema
- `frontend-next/app/[locale]/page.tsx` — 删除内联 WebSite schema（移到 layout）
- `frontend-next/app/[locale]/courses/[slug]/page.tsx` — 替换内联 Course schema + 添加 BreadcrumbList
- `frontend-next/app/[locale]/news/[slug]/page.tsx` — 替换内联 NewsArticle schema + 添加 BreadcrumbList
- `frontend-next/app/[locale]/campuses/[slug]/page.tsx` — 新增 LocalBusiness + BreadcrumbList
- `frontend-next/app/[locale]/teachers/[slug]/page.tsx` — 新增 Person + BreadcrumbList
- `frontend-next/app/[locale]/faq/page.tsx` — 提取内联 FAQPage 到生成器 + 添加 BreadcrumbList
- `frontend-next/app/[locale]/courses/page.tsx` — 添加 BreadcrumbList
- `frontend-next/app/[locale]/news/page.tsx` — 添加 BreadcrumbList
- `frontend-next/app/[locale]/campuses/page.tsx` — 添加 BreadcrumbList
- `frontend-next/app/[locale]/teachers/page.tsx` — 添加 BreadcrumbList
- `frontend-next/app/[locale]/[slug]/page.tsx` — 添加 BreadcrumbList
- `frontend-next/app/[locale]/refund-policy/page.tsx` — 添加 BreadcrumbList
- `frontend-next/app/[locale]/privacy-policy/page.tsx` — 添加 BreadcrumbList
- `frontend-next/app/[locale]/user-agreement/page.tsx` — 添加 BreadcrumbList
- `frontend-next/app/[locale]/appointment/page.tsx` — 添加 BreadcrumbList（如存在）
- `frontend-next/app/[locale]/contact/page.tsx` — 添加 BreadcrumbList（如存在）
- `frontend-next/app/sitemap.ts` — 新增 campuses/teachers 动态 URL
- `frontend-next/components/course/CourseSearchPanel.tsx` — 移除 `<Seo>` 使用

**删除的文件：**
- `frontend-next/components/Seo.tsx`

**创建的文件：**
- `frontend-next/e2e/seo.spec.ts` — SEO E2E 测试

---

## 任务 1：Schema 生成器 Part 1（Organization、WebSite、BreadcrumbList、FAQPage）

**文件：**
- 修改：`frontend-next/lib/seo.ts`
- 测试：`frontend-next/lib/__tests__/seo.test.ts`

- [ ] **步骤 1：编写 4 个生成器的失败测试**

在 `frontend-next/lib/__tests__/seo.test.ts` 文件末尾追加：

```typescript
describe('buildWebSiteSchema', () => {
  beforeEach(() => {
    process.env.NEXT_PUBLIC_SITE_URL = 'https://example.com';
  });
  afterEach(() => {
    delete process.env.NEXT_PUBLIC_SITE_URL;
  });

  it('returns WebSite schema with name and url', () => {
    const settings = { name: '佑森小课堂' } as any;
    const schema = buildWebSiteSchema(settings, 'zh-CN');
    expect(schema['@type']).toBe('WebSite');
    expect(schema['@context']).toBe('https://schema.org');
    expect(schema['name']).toBe('佑森小课堂');
    expect(schema['url']).toBe('https://example.com');
  });

  it('does not include potentialAction (no global search)', () => {
    const settings = { name: 'Test' } as any;
    const schema = buildWebSiteSchema(settings, 'zh-CN');
    expect(schema['potentialAction']).toBeUndefined();
  });

  it('uses en-US url prefix for en-US locale', () => {
    const settings = { name: 'Test' } as any;
    const schema = buildWebSiteSchema(settings, 'en-US');
    expect(schema['url']).toBe('https://example.com/en-US');
  });
});

describe('buildOrganizationSchema', () => {
  beforeEach(() => {
    process.env.NEXT_PUBLIC_SITE_URL = 'https://example.com';
  });
  afterEach(() => {
    delete process.env.NEXT_PUBLIC_SITE_URL;
  });

  it('returns EducationalOrganization schema with core fields', () => {
    const settings = {
      name: '佑森小课堂',
      phone: '027-12345678',
      email: 'test@example.com',
      address: '武汉市武昌区',
      logo: { url: '/logo.png', alternativeText: 'logo' },
    } as any;
    const schema = buildOrganizationSchema(settings, [], 'zh-CN');
    expect(schema['@type']).toBe('EducationalOrganization');
    expect(schema['name']).toBe('佑森小课堂');
    expect(schema['url']).toBe('https://example.com');
    expect(schema['telephone']).toBe('027-12345678');
    expect(schema['email']).toBe('test@example.com');
    expect(schema['logo']).toBe('https://example.com/logo.png');
  });

  it('extracts sameAs from socialLinks filtering only http URLs', () => {
    const settings = { name: 'Test' } as any;
    const socialLinks = [
      { platform: 'wechat', url: 'wechat_id_123', label: 'WeChat' },
      { platform: 'weibo', url: 'https://weibo.com/yousen', label: 'Weibo' },
      { platform: 'douyin', url: 'https://douyin.com/yousen', label: 'Douyin' },
    ] as any;
    const schema = buildOrganizationSchema(settings, socialLinks, 'zh-CN');
    expect(schema['sameAs']).toEqual([
      'https://weibo.com/yousen',
      'https://douyin.com/yousen',
    ]);
  });

  it('handles empty socialLinks gracefully', () => {
    const settings = { name: 'Test' } as any;
    const schema = buildOrganizationSchema(settings, [], 'zh-CN');
    expect(schema['sameAs']).toBeUndefined();
  });

  it('builds PostalAddress structure from address string', () => {
    const settings = {
      name: 'Test',
      address: '武汉市武昌区和平大道100号',
    } as any;
    const schema = buildOrganizationSchema(settings, [], 'zh-CN');
    expect(schema['address']).toEqual({
      '@type': 'PostalAddress',
      streetAddress: '武汉市武昌区和平大道100号',
    });
  });

  it('omits telephone/email/logo when not provided', () => {
    const settings = { name: 'Test' } as any;
    const schema = buildOrganizationSchema(settings, [], 'zh-CN');
    expect(schema['telephone']).toBeUndefined();
    expect(schema['email']).toBeUndefined();
    expect(schema['logo']).toBeUndefined();
  });
});

describe('buildBreadcrumbSchema', () => {
  beforeEach(() => {
    process.env.NEXT_PUBLIC_SITE_URL = 'https://example.com';
  });
  afterEach(() => {
    delete process.env.NEXT_PUBLIC_SITE_URL;
  });

  it('returns BreadcrumbList with correct itemListElement', () => {
    const items = [
      { name: '首页', url: '/' },
      { name: '课程', url: '/courses' },
      { name: '拼音班', url: '/courses/pinyin' },
    ];
    const schema = buildBreadcrumbSchema(items, 'zh-CN');
    expect(schema['@type']).toBe('BreadcrumbList');
    expect(schema['itemListElement']).toHaveLength(3);
    expect(schema['itemListElement'][0]).toEqual({
      '@type': 'ListItem',
      position: 1,
      name: '首页',
      item: 'https://example.com/',
    });
    expect(schema['itemListElement'][1]).toEqual({
      '@type': 'ListItem',
      position: 2,
      name: '课程',
      item: 'https://example.com/courses',
    });
    expect(schema['itemListElement'][2]).toEqual({
      '@type': 'ListItem',
      position: 3,
      name: '拼音班',
      item: 'https://example.com/courses/pinyin',
    });
  });

  it('adds /en-US prefix for en-US locale', () => {
    const items = [{ name: 'Home', url: '/' }];
    const schema = buildBreadcrumbSchema(items, 'en-US');
    expect(schema['itemListElement'][0]['item']).toBe('https://example.com/en-US/');
  });

  it('handles empty items array', () => {
    const schema = buildBreadcrumbSchema([], 'zh-CN');
    expect(schema['itemListElement']).toEqual([]);
  });
});

describe('buildFaqPageSchema', () => {
  it('returns FAQPage with Question entities', () => {
    const faqItems = [
      { question: '什么是幼小衔接？', answer: '幼小衔接是幼儿园到小学的过渡期教育。' },
      { question: '课程如何安排？', answer: '每周3次课，每次2小时。' },
    ] as any;
    const schema = buildFaqPageSchema(faqItems);
    expect(schema['@type']).toBe('FAQPage');
    expect(schema['mainEntity']).toHaveLength(2);
    expect(schema['mainEntity'][0]).toEqual({
      '@type': 'Question',
      name: '什么是幼小衔接？',
      acceptedAnswer: {
        '@type': 'Answer',
        text: '幼小衔接是幼儿园到小学的过渡期教育。',
      },
    });
  });

  it('handles empty faqItems array', () => {
    const schema = buildFaqPageSchema([]);
    expect(schema['mainEntity']).toEqual([]);
  });
});
```

同时在测试文件顶部更新导入：

```typescript
import { buildMetadata, buildWebSiteSchema, buildOrganizationSchema, buildBreadcrumbSchema, buildFaqPageSchema } from '../seo';
```

- [ ] **步骤 2：运行测试验证失败**

运行：
```bash
cd frontend-next && npx vitest run lib/__tests__/seo.test.ts
```
预期：FAIL，报错 `buildWebSiteSchema is not a function` 或类似导入错误

- [ ] **步骤 3：实现 4 个生成器函数**

在 `frontend-next/lib/seo.ts` 文件末尾（现有 `buildJsonLd` 函数之后）追加：

```typescript
import type {
  SiteSettings,
  SocialLink,
  Campus,
  Product,
  NewsArticle,
  Teacher,
  FaqItem,
  Locale,
} from './api';

/**
 * 构建 WebSite schema（全站根网站实体）
 */
export function buildWebSiteSchema(
  settings: Pick<SiteSettings, 'name'>,
  locale: Locale
): Record<string, unknown> {
  const baseUrl = process.env.NEXT_PUBLIC_SITE_URL || 'http://localhost:3000';
  const url = locale === 'en-US' ? `${baseUrl}/en-US` : baseUrl;
  return {
    '@context': 'https://schema.org',
    '@type': 'WebSite',
    name: settings.name,
    url,
  };
}

/**
 * 构建 Organization schema（教育机构实体）
 * sameAs 仅包含以 http 开头的社交链接 URL（过滤微信号等非 URL 值）
 */
export function buildOrganizationSchema(
  settings: Pick<SiteSettings, 'name' | 'phone' | 'email' | 'address' | 'logo'>,
  socialLinks: SocialLink[],
  locale: Locale
): Record<string, unknown> {
  const baseUrl = process.env.NEXT_PUBLIC_SITE_URL || 'http://localhost:3000';
  const url = locale === 'en-US' ? `${baseUrl}/en-US` : baseUrl;
  const schema: Record<string, unknown> = {
    '@context': 'https://schema.org',
    '@type': 'EducationalOrganization',
    name: settings.name,
    url,
  };

  if (settings.phone) schema.telephone = settings.phone;
  if (settings.email) schema.email = settings.email;
  if (settings.address) {
    schema.address = {
      '@type': 'PostalAddress',
      streetAddress: settings.address,
    };
  }
  if (settings.logo?.url) {
    schema.logo = getImageUrl(settings.logo);
  }

  const sameAs = socialLinks
    .map((s) => s.url)
    .filter((url) => url && url.startsWith('http'));
  if (sameAs.length > 0) {
    schema.sameAs = sameAs;
  }

  return schema;
}

/**
 * 构建 BreadcrumbList schema（面包屑导航）
 * 调用方传入的 url 为相对路径（如 '/courses'），生成器根据 locale 自动加 /en-US 前缀并拼接 baseUrl
 */
export function buildBreadcrumbSchema(
  items: { name: string; url: string }[],
  locale: Locale
): Record<string, unknown> {
  const baseUrl = process.env.NEXT_PUBLIC_SITE_URL || 'http://localhost:3000';
  const prefix = locale === 'en-US' ? '/en-US' : '';
  return {
    '@context': 'https://schema.org',
    '@type': 'BreadcrumbList',
    itemListElement: items.map((item, index) => ({
      '@type': 'ListItem',
      position: index + 1,
      name: item.name,
      item: `${baseUrl}${prefix}${item.url}`,
    })),
  };
}

/**
 * 构建 FAQPage schema（常见问题页）
 */
export function buildFaqPageSchema(
  faqItems: FaqItem[]
): Record<string, unknown> {
  return {
    '@context': 'https://schema.org',
    '@type': 'FAQPage',
    mainEntity: faqItems.map((f) => ({
      '@type': 'Question',
      name: f.question,
      acceptedAnswer: {
        '@type': 'Answer',
        text: f.answer,
      },
    })),
  };
}
```

- [ ] **步骤 4：运行测试验证通过**

运行：
```bash
cd frontend-next && npx vitest run lib/__tests__/seo.test.ts
```
预期：PASS，所有测试用例通过

- [ ] **步骤 5：Commit**

```bash
git add frontend-next/lib/seo.ts frontend-next/lib/__tests__/seo.test.ts
git commit -m "feat(seo): add WebSite, Organization, BreadcrumbList, FAQPage schema generators

Add 4 JSON-LD schema generator functions to lib/seo.ts:
- buildWebSiteSchema: site entity with locale-aware url
- buildOrganizationSchema: EducationalOrganization with sameAs from social links
- buildBreadcrumbSchema: BreadcrumbList with locale prefix
- buildFaqPageSchema: FAQPage with Question/Answer entities

Each function returns Record<string, unknown> for buildJsonLd() serialization.
Unit tests cover @type, locale handling, empty data, and field extraction."
```

---

## 任务 2：Schema 生成器 Part 2（LocalBusiness、Person、Course、NewsArticle）

**文件：**
- 修改：`frontend-next/lib/seo.ts`
- 测试：`frontend-next/lib/__tests__/seo.test.ts`

- [ ] **步骤 1：编写 4 个生成器的失败测试**

在 `frontend-next/lib/__tests__/seo.test.ts` 文件末尾追加：

```typescript
describe('buildLocalBusinessSchema', () => {
  beforeEach(() => {
    process.env.NEXT_PUBLIC_SITE_URL = 'https://example.com';
  });
  afterEach(() => {
    delete process.env.NEXT_PUBLIC_SITE_URL;
  });

  it('returns LocalBusiness + EducationalOrganization dual type', () => {
    const campus = {
      name: '百步亭校区',
      slug: 'baibuting',
      address: '武汉市江岸区百步亭花园路',
      phone: '027-82345678',
      businessHours: '周一至周五 8:00-18:00',
      coverImage: { url: '/campus.jpg', alternativeText: 'campus' },
    } as any;
    const schema = buildLocalBusinessSchema(campus, 'zh-CN');
    expect(schema['@type']).toEqual(['LocalBusiness', 'EducationalOrganization']);
    expect(schema['name']).toBe('百步亭校区');
    expect(schema['address']).toEqual({
      '@type': 'PostalAddress',
      streetAddress: '武汉市江岸区百步亭花园路',
    });
    expect(schema['telephone']).toBe('027-82345678');
    expect(schema['openingHours']).toBe('周一至周五 8:00-18:00');
    expect(schema['url']).toBe('https://example.com/campuses/baibuting');
  });

  it('adds /en-US prefix to url for en-US locale', () => {
    const campus = { name: 'Test', slug: 'test', address: 'addr' } as any;
    const schema = buildLocalBusinessSchema(campus, 'en-US');
    expect(schema['url']).toBe('https://example.com/en-US/campuses/test');
  });

  it('omits telephone/openingHours/image when not provided', () => {
    const campus = { name: 'Test', slug: 'test', address: 'addr' } as any;
    const schema = buildLocalBusinessSchema(campus, 'zh-CN');
    expect(schema['telephone']).toBeUndefined();
    expect(schema['openingHours']).toBeUndefined();
    expect(schema['image']).toBeUndefined();
  });

  it('includes image from coverImage when available', () => {
    const campus = {
      name: 'Test',
      slug: 'test',
      address: 'addr',
      coverImage: { url: '/img.jpg' },
    } as any;
    const schema = buildLocalBusinessSchema(campus, 'zh-CN');
    expect(schema['image']).toBe('https://example.com/img.jpg');
  });
});

describe('buildPersonSchema', () => {
  beforeEach(() => {
    process.env.NEXT_PUBLIC_SITE_URL = 'https://example.com';
  });
  afterEach(() => {
    delete process.env.NEXT_PUBLIC_SITE_URL;
  });

  it('returns Person schema with core fields', () => {
    const teacher = {
      name: '张老师',
      title: '高级教师',
      slug: 'zhang-laoshi',
      avatar: { url: '/avatar.jpg', alternativeText: 'avatar' },
      subject: 'pinyin',
      teachingFeatures: '趣味拼音教学',
      achievements: ['市级优秀教师', '教学比赛一等奖'],
    } as any;
    const schema = buildPersonSchema(teacher, 'zh-CN');
    expect(schema['@type']).toBe('Person');
    expect(schema['name']).toBe('张老师');
    expect(schema['jobTitle']).toBe('高级教师');
    expect(schema['image']).toBe('https://example.com/avatar.jpg');
    expect(schema['url']).toBe('https://example.com/teachers/zhang-laoshi');
    expect(schema['worksFor']).toEqual({
      '@type': 'EducationalOrganization',
      name: '佑森小课堂',
    });
    expect(schema['knowsAbout']).toEqual(['市级优秀教师', '教学比赛一等奖']);
  });

  it('omits jobTitle/image/knowsAbout when not provided', () => {
    const teacher = { name: 'Test', slug: 'test' } as any;
    const schema = buildPersonSchema(teacher, 'zh-CN');
    expect(schema['jobTitle']).toBeUndefined();
    expect(schema['image']).toBeUndefined();
    expect(schema['knowsAbout']).toBeUndefined();
  });

  it('omits knowsAbout when achievements is empty array', () => {
    const teacher = { name: 'Test', slug: 'test', achievements: [] } as any;
    const schema = buildPersonSchema(teacher, 'zh-CN');
    expect(schema['knowsAbout']).toBeUndefined();
  });
});

describe('buildCourseSchema', () => {
  beforeEach(() => {
    process.env.NEXT_PUBLIC_SITE_URL = 'https://example.com';
  });
  afterEach(() => {
    delete process.env.NEXT_PUBLIC_SITE_URL;
  });

  it('returns Course schema with provider and offers', () => {
    const product = {
      name: '拼音全能班',
      slug: 'pinyin',
      description: '系统学习汉语拼音',
      price: 2980,
    } as any;
    const settings = { name: '佑森小课堂' } as any;
    const schema = buildCourseSchema(product, settings, 'zh-CN');
    expect(schema['@type']).toBe('Course');
    expect(schema['name']).toBe('拼音全能班');
    expect(schema['description']).toBe('系统学习汉语拼音');
    expect(schema['provider']).toEqual({
      '@type': 'EducationalOrganization',
      name: '佑森小课堂',
    });
    expect(schema['offers']).toEqual({
      '@type': 'Offer',
      price: 2980,
      priceCurrency: 'CNY',
    });
  });

  it('omits offers when price is missing', () => {
    const product = { name: 'Test', slug: 'test', description: 'desc' } as any;
    const settings = { name: 'Test' } as any;
    const schema = buildCourseSchema(product, settings, 'zh-CN');
    expect(schema['offers']).toBeUndefined();
  });

  it('falls back to shortDescription when description missing', () => {
    const product = {
      name: 'Test',
      slug: 'test',
      shortDescription: 'short desc',
    } as any;
    const settings = { name: 'Test' } as any;
    const schema = buildCourseSchema(product, settings, 'zh-CN');
    expect(schema['description']).toBe('short desc');
  });
});

describe('buildNewsArticleSchema', () => {
  beforeEach(() => {
    process.env.NEXT_PUBLIC_SITE_URL = 'https://example.com';
  });
  afterEach(() => {
    delete process.env.NEXT_PUBLIC_SITE_URL;
  });

  it('returns NewsArticle schema with author and publisher', () => {
    const news = {
      title: '佑森小课堂开学通知',
      slug: 'opening-notice',
      excerpt: '2026年春季班开始报名',
      content: '<p>详细内容</p>',
      publishedAt: '2026-01-15T10:00:00Z',
      coverImage: { url: '/cover.jpg' },
    } as any;
    const settings = { name: '佑森小课堂' } as any;
    const schema = buildNewsArticleSchema(news, settings, 'zh-CN');
    expect(schema['@type']).toBe('NewsArticle');
    expect(schema['headline']).toBe('佑森小课堂开学通知');
    expect(schema['datePublished']).toBe('2026-01-15T10:00:00Z');
    expect(schema['dateModified']).toBe('2026-01-15T10:00:00Z');
    expect(schema['author']).toEqual({
      '@type': 'Organization',
      name: '佑森小课堂',
    });
    expect(schema['publisher']).toEqual({
      '@type': 'Organization',
      name: '佑森小课堂',
    });
    expect(schema['image']).toBe('https://example.com/cover.jpg');
  });

  it('uses excerpt as description when available', () => {
    const news = {
      title: 'Test',
      slug: 'test',
      excerpt: 'excerpt text',
      publishedAt: '2026-01-15T10:00:00Z',
    } as any;
    const settings = { name: 'Test' } as any;
    const schema = buildNewsArticleSchema(news, settings, 'zh-CN');
    expect(schema['description']).toBe('excerpt text');
  });

  it('omits image when coverImage missing', () => {
    const news = {
      title: 'Test',
      slug: 'test',
      publishedAt: '2026-01-15T10:00:00Z',
    } as any;
    const settings = { name: 'Test' } as any;
    const schema = buildNewsArticleSchema(news, settings, 'zh-CN');
    expect(schema['image']).toBeUndefined();
  });
});
```

更新测试文件顶部导入：

```typescript
import {
  buildMetadata,
  buildWebSiteSchema,
  buildOrganizationSchema,
  buildBreadcrumbSchema,
  buildFaqPageSchema,
  buildLocalBusinessSchema,
  buildPersonSchema,
  buildCourseSchema,
  buildNewsArticleSchema,
} from '../seo';
```

- [ ] **步骤 2：运行测试验证失败**

运行：
```bash
cd frontend-next && npx vitest run lib/__tests__/seo.test.ts
```
预期：FAIL，报错 `buildLocalBusinessSchema is not a function`

- [ ] **步骤 3：实现 4 个生成器函数**

在 `frontend-next/lib/seo.ts` 文件末尾追加：

```typescript
/**
 * 构建 LocalBusiness schema（校区本地商业实体）
 * 使用双类型 ['LocalBusiness', 'EducationalOrganization'] 兼顾本地 SEO 和教育权威
 */
export function buildLocalBusinessSchema(
  campus: Pick<Campus, 'name' | 'slug' | 'address' | 'phone' | 'businessHours' | 'coverImage'>,
  locale: Locale
): Record<string, unknown> {
  const baseUrl = process.env.NEXT_PUBLIC_SITE_URL || 'http://localhost:3000';
  const prefix = locale === 'en-US' ? '/en-US' : '';
  const schema: Record<string, unknown> = {
    '@context': 'https://schema.org',
    '@type': ['LocalBusiness', 'EducationalOrganization'],
    name: campus.name,
    url: `${baseUrl}${prefix}/campuses/${campus.slug}`,
  };

  if (campus.address) {
    schema.address = {
      '@type': 'PostalAddress',
      streetAddress: campus.address,
    };
  }
  if (campus.phone) schema.telephone = campus.phone;
  if (campus.businessHours) schema.openingHours = campus.businessHours;
  if (campus.coverImage?.url) {
    schema.image = getImageUrl(campus.coverImage);
  }

  return schema;
}

/**
 * 构建 Person schema（教师实体）
 */
export function buildPersonSchema(
  teacher: Pick<Teacher, 'name' | 'title' | 'slug' | 'avatar' | 'achievements'>,
  locale: Locale
): Record<string, unknown> {
  const baseUrl = process.env.NEXT_PUBLIC_SITE_URL || 'http://localhost:3000';
  const prefix = locale === 'en-US' ? '/en-US' : '';
  const schema: Record<string, unknown> = {
    '@context': 'https://schema.org',
    '@type': 'Person',
    name: teacher.name,
    url: `${baseUrl}${prefix}/teachers/${teacher.slug}`,
    worksFor: {
      '@type': 'EducationalOrganization',
      name: '佑森小课堂',
    },
  };

  if (teacher.title) schema.jobTitle = teacher.title;
  if (teacher.avatar?.url) {
    schema.image = getImageUrl(teacher.avatar);
  }
  const achievements = Array.isArray(teacher.achievements)
    ? teacher.achievements.filter(Boolean)
    : [];
  if (achievements.length > 0) {
    schema.knowsAbout = achievements;
  }

  return schema;
}

/**
 * 构建 Course schema（课程实体，增强版）
 * provider 为 EducationalOrganization，offers 含 price + priceCurrency
 */
export function buildCourseSchema(
  product: Pick<Product, 'name' | 'slug' | 'description' | 'shortDescription' | 'price'>,
  settings: Pick<SiteSettings, 'name'>,
  locale: Locale
): Record<string, unknown> {
  const baseUrl = process.env.NEXT_PUBLIC_SITE_URL || 'http://localhost:3000';
  const prefix = locale === 'en-US' ? '/en-US' : '';
  const description = product.description || product.shortDescription || '';
  const schema: Record<string, unknown> = {
    '@context': 'https://schema.org',
    '@type': 'Course',
    name: product.name,
    description,
    url: `${baseUrl}${prefix}/courses/${product.slug}`,
    provider: {
      '@type': 'EducationalOrganization',
      name: settings.name,
    },
  };

  if (typeof product.price === 'number') {
    schema.offers = {
      '@type': 'Offer',
      price: product.price,
      priceCurrency: 'CNY',
    };
  }

  return schema;
}

/**
 * 构建 NewsArticle schema（新闻文章实体，增强版）
 * author 和 publisher 均为 Organization 类型
 */
export function buildNewsArticleSchema(
  news: Pick<NewsArticle, 'title' | 'slug' | 'excerpt' | 'publishedAt' | 'coverImage'>,
  settings: Pick<SiteSettings, 'name'>,
  locale: Locale
): Record<string, unknown> {
  const baseUrl = process.env.NEXT_PUBLIC_SITE_URL || 'http://localhost:3000';
  const prefix = locale === 'en-US' ? '/en-US' : '';
  const schema: Record<string, unknown> = {
    '@context': 'https://schema.org',
    '@type': 'NewsArticle',
    headline: news.title,
    datePublished: news.publishedAt,
    dateModified: news.publishedAt,
    author: {
      '@type': 'Organization',
      name: settings.name,
    },
    publisher: {
      '@type': 'Organization',
      name: settings.name,
    },
  };

  if (news.excerpt) schema.description = news.excerpt;
  if (news.coverImage?.url) {
    schema.image = getImageUrl(news.coverImage);
  }

  return schema;
}
```

- [ ] **步骤 4：运行测试验证通过**

运行：
```bash
cd frontend-next && npx vitest run lib/__tests__/seo.test.ts
```
预期：PASS，所有测试用例通过

- [ ] **步骤 5：Commit**

```bash
git add frontend-next/lib/seo.ts frontend-next/lib/__tests__/seo.test.ts
git commit -m "feat(seo): add LocalBusiness, Person, Course, NewsArticle schema generators

Add 4 JSON-LD schema generator functions to lib/seo.ts:
- buildLocalBusinessSchema: dual @type ['LocalBusiness', 'EducationalOrganization']
  for 6 campuses with address/telephone/openingHours/image
- buildPersonSchema: teacher entity with worksFor/knowsAbout
- buildCourseSchema: enhanced Course with provider (EducationalOrganization) and offers
- buildNewsArticleSchema: enhanced NewsArticle with author/publisher (Organization)

Unit tests cover @type, locale-aware urls, field optionality, and edge cases."
```

---

## 任务 3：全局 Schema 注入（layout.tsx）

**文件：**
- 修改：`frontend-next/app/[locale]/layout.tsx`
- 修改：`frontend-next/app/[locale]/page.tsx`

- [ ] **步骤 1：在 layout.tsx 注入 Organization + WebSite schema**

在 `frontend-next/app/[locale]/layout.tsx` 中：

1. 更新导入（第 5 行附近，在 `getImageUrl` 之后添加 schema 生成器导入）：

```typescript
import { getSiteSettings, getNavigationTree, getFooter, getImageUrl, type Locale } from '@/lib/api';
import { buildWebSiteSchema, buildOrganizationSchema, buildJsonLd } from '@/lib/seo';
```

2. 在 `LocaleLayout` 函数中，`return` 语句之前（约第 134 行，`cmsParsedUrl` 之后），添加 schema 生成代码：

```typescript
  const websiteSchema = settings
    ? buildWebSiteSchema(settings, locale as Locale)
    : null;
  const orgSchema = settings
    ? buildOrganizationSchema(
        settings,
        footer?.socialLinks || [],
        locale as Locale
      )
    : null;
```

3. 在 `<body>` 标签内、`<NextIntlClientProvider>` 之前（约第 148 行），插入 schema script 标签：

```tsx
      <body>
        {websiteSchema && (
          <script
            type="application/ld+json"
            dangerouslySetInnerHTML={{ __html: buildJsonLd(websiteSchema) }}
          />
        )}
        {orgSchema && (
          <script
            type="application/ld+json"
            dangerouslySetInnerHTML={{ __html: buildJsonLd(orgSchema) }}
          />
        )}
        <NextIntlClientProvider locale={locale} messages={messages}>
```

- [ ] **步骤 2：从首页删除内联 WebSite schema**

在 `frontend-next/app/[locale]/page.tsx` 中：

1. 删除 `buildJsonLd` 导入（第 2 行），保留 `buildMetadata`：

```typescript
import { buildMetadata } from '@/lib/seo';
```

2. 删除 `websiteJsonLd` 变量声明（约第 42-48 行）：

```typescript
  // 删除以下代码：
  // const websiteJsonLd = {
  //   '@context': 'https://schema.org',
  //   '@type': 'WebSite',
  //   name: siteName,
  //   url: siteUrl,
  // };
```

3. 删除 `<script>` 标签和外层 Fragment（约第 50-55 行），直接返回 sections：

```tsx
  return (
    <>
      {sections.map((section, index) => (
        <SectionRenderer
          key={`${section.__component}-${section.id}-${index}`}
          section={section}
        />
      ))}
    </>
  );
```

4. 同时删除未使用的 `siteUrl` 变量（约第 40 行，`const siteUrl = ...`），因为 WebSite schema 已移到 layout。

- [ ] **步骤 3：运行测试验证无回归**

运行：
```bash
cd frontend-next && npx vitest run
```
预期：PASS，所有现有测试通过（无新增测试，仅重构）

- [ ] **步骤 4：Commit**

```bash
git add frontend-next/app/[locale]/layout.tsx frontend-next/app/[locale]/page.tsx
git commit -m "feat(seo): inject Organization + WebSite schema globally in layout

- Move WebSite schema from homepage to layout.tsx (global injection)
- Add Organization (EducationalOrganization) schema in layout.tsx
- Remove inline WebSite schema from app/[locale]/page.tsx
- Both schemas use buildWebSiteSchema/buildOrganizationSchema generators

Organization sameAs extracted from Footer socialLinks (http URLs only)."
```

---

## 任务 4：详情页 Schema 注入（courses、news、campuses、teachers、faq）

**文件：**
- 修改：`frontend-next/app/[locale]/courses/[slug]/page.tsx`
- 修改：`frontend-next/app/[locale]/news/[slug]/page.tsx`
- 修改：`frontend-next/app/[locale]/campuses/[slug]/page.tsx`
- 修改：`frontend-next/app/[locale]/teachers/[slug]/page.tsx`
- 修改：`frontend-next/app/[locale]/faq/page.tsx`

- [ ] **步骤 1：课程详情页 — 替换内联 Course schema + 添加 BreadcrumbList**

在 `frontend-next/app/[locale]/courses/[slug]/page.tsx` 中：

1. 更新导入（第 4 行）：

```typescript
import { buildMetadata, buildJsonLd, buildCourseSchema, buildBreadcrumbSchema } from '@/lib/seo';
import { getProducts, getProductBySlug, getSiteSettings, type Locale } from '@/lib/api';
```

2. 在 `CourseDetailPage` 函数中，`notFound()` 之后、`return` 之前，获取 settings 并生成 schema（约第 54 行之后插入）：

```typescript
  if (!product) {
    notFound();
  }

  const { data: settingsData } = await getSiteSettings(locale as Locale).catch(() => ({ data: [] as never[] }));
  const settings = Array.isArray(settingsData) ? settingsData[0] : settingsData;

  const courseSchema = buildCourseSchema(product, settings || { name: '佑森小课堂' }, locale as Locale);
  const breadcrumbSchema = buildBreadcrumbSchema(
    [
      { name: locale === 'en-US' ? 'Home' : '首页', url: '/' },
      { name: locale === 'en-US' ? 'Courses' : '课程', url: '/courses' },
      { name: product.name, url: `/courses/${product.slug}` },
    ],
    locale as Locale
  );

  return (
    <>
      <script
        type="application/ld+json"
        dangerouslySetInnerHTML={{ __html: buildJsonLd(courseSchema) }}
      />
      <script
        type="application/ld+json"
        dangerouslySetInnerHTML={{ __html: buildJsonLd(breadcrumbSchema) }}
      />
      <CourseHeader product={product} />
```

3. 删除旧的内联 `courseJsonLd` 变量（约第 56-61 行）：

```typescript
  // 删除：
  // const courseJsonLd = {
  //   '@context': 'https://schema.org',
  //   '@type': 'Course',
  //   name: product.name,
  //   description: product.description || product.shortDescription || '',
  // };
```

- [ ] **步骤 2：新闻详情页 — 替换内联 NewsArticle schema + 添加 BreadcrumbList**

在 `frontend-next/app/[locale]/news/[slug]/page.tsx` 中：

1. 更新导入（第 6 行）：

```typescript
import { buildMetadata, buildJsonLd, buildNewsArticleSchema, buildBreadcrumbSchema } from '@/lib/seo';
import { getNews, getNewsBySlug, getNewsCategoryLabel, getImageUrl, getSiteSettings, type Locale } from '@/lib/api';
```

2. 在 `NewsDetailPage` 函数中，`notFound()` 之后、`return` 之前（约第 77 行之后），添加 schema 生成：

```typescript
  if (!news) {
    notFound();
  }

  const { data: settingsData } = await getSiteSettings(locale as Locale).catch(() => ({ data: [] as never[] }));
  const settings = Array.isArray(settingsData) ? settingsData[0] : settingsData;
  const newsSettings = settings || { name: '佑森小课堂' };

  const newsSchema = buildNewsArticleSchema(news, newsSettings, locale as Locale);
  const breadcrumbSchema = buildBreadcrumbSchema(
    [
      { name: locale === 'en-US' ? 'Home' : '首页', url: '/' },
      { name: locale === 'en-US' ? 'News' : '新闻动态', url: '/news' },
      { name: news.title, url: `/news/${news.slug}` },
    ],
    locale as Locale
  );

  const { title, content, coverImage, category, publishedAt, viewCount } = news;
```

3. 删除旧的内联 `articleJsonLd` 变量（约第 83-89 行）。

4. 在 return 的 `<div>` 内部、`<script>` 标签处，替换为两个 script 标签：

```tsx
      <script
        type="application/ld+json"
        dangerouslySetInnerHTML={{ __html: buildJsonLd(newsSchema) }}
      />
      <script
        type="application/ld+json"
        dangerouslySetInnerHTML={{ __html: buildJsonLd(breadcrumbSchema) }}
      />
```

- [ ] **步骤 3：校区详情页 — 新增 LocalBusiness + BreadcrumbList**

在 `frontend-next/app/[locale]/campuses/[slug]/page.tsx` 中：

1. 更新导入（第 4 行）：

```typescript
import { buildMetadata, buildJsonLd, buildLocalBusinessSchema, buildBreadcrumbSchema } from '@/lib/seo';
```

2. 在 `CampusDetailPage` 函数中，`notFound()` 之后、`return` 之前（约第 51 行之后），添加 schema 生成：

```typescript
  if (!campus) {
    notFound();
  }

  const localBusinessSchema = buildLocalBusinessSchema(campus, locale as Locale);
  const breadcrumbSchema = buildBreadcrumbSchema(
    [
      { name: locale === 'en-US' ? 'Home' : '首页', url: '/' },
      { name: locale === 'en-US' ? 'Campuses' : '校区分布', url: '/campuses' },
      { name: campus.name, url: `/campuses/${campus.slug}` },
    ],
    locale as Locale
  );

  return (
    <div className="pt-[120px] pb-16">
      <script
        type="application/ld+json"
        dangerouslySetInnerHTML={{ __html: buildJsonLd(localBusinessSchema) }}
      />
      <script
        type="application/ld+json"
        dangerouslySetInnerHTML={{ __html: buildJsonLd(breadcrumbSchema) }}
      />
      <div className="max-w-[1400px] mx-auto px-8">
```

- [ ] **步骤 4：教师详情页 — 新增 Person + BreadcrumbList**

在 `frontend-next/app/[locale]/teachers/[slug]/page.tsx` 中：

1. 更新导入（第 5 行）：

```typescript
import { buildMetadata, buildJsonLd, buildPersonSchema, buildBreadcrumbSchema } from '@/lib/seo';
```

2. 在 `TeacherDetailPage` 函数中，`notFound()` 之后、`return` 之前（约第 56 行之后），添加 schema 生成：

```typescript
  if (!teacher) {
    notFound();
  }

  const personSchema = buildPersonSchema(teacher, locale as Locale);
  const breadcrumbSchema = buildBreadcrumbSchema(
    [
      { name: locale === 'en-US' ? 'Home' : '首页', url: '/' },
      { name: locale === 'en-US' ? 'Teachers' : '师资团队', url: '/teachers' },
      { name: teacher.name, url: `/teachers/${teacher.slug}` },
    ],
    locale as Locale
  );

  const achievementList = Array.isArray(teacher.achievements)
    ? teacher.achievements
    : [];

  return (
    <div className="pt-[120px] pb-24 min-h-screen" style={{ background: '#FAFAFA' }}>
      <script
        type="application/ld+json"
        dangerouslySetInnerHTML={{ __html: buildJsonLd(personSchema) }}
      />
      <script
        type="application/ld+json"
        dangerouslySetInnerHTML={{ __html: buildJsonLd(breadcrumbSchema) }}
      />
      <div className="max-w-[1200px] mx-auto px-8">
```

- [ ] **步骤 5：FAQ 页 — 提取内联 schema 到生成器 + 添加 BreadcrumbList**

在 `frontend-next/app/[locale]/faq/page.tsx` 中：

1. 更新导入（第 2 行）：

```typescript
import { buildMetadata, buildJsonLd, buildFaqPageSchema, buildBreadcrumbSchema } from '@/lib/seo';
```

2. 替换内联 `faqJsonLd` 变量（约第 32-43 行）为生成器调用：

```typescript
  const faqJsonLd = buildFaqPageSchema(faqItems);
  const breadcrumbSchema = buildBreadcrumbSchema(
    [
      { name: locale === 'en-US' ? 'Home' : '首页', url: '/' },
      { name: locale === 'en-US' ? 'FAQ' : '常见问题', url: '/faq' },
    ],
    locale as Locale
  );
```

3. 在 return 中添加第二个 script 标签（约第 47-50 行）：

```tsx
      <script
        type="application/ld+json"
        dangerouslySetInnerHTML={{ __html: buildJsonLd(faqJsonLd) }}
      />
      <script
        type="application/ld+json"
        dangerouslySetInnerHTML={{ __html: buildJsonLd(breadcrumbSchema) }}
      />
```

- [ ] **步骤 6：运行测试验证无回归**

运行：
```bash
cd frontend-next && npx vitest run
```
预期：PASS，所有现有测试通过

- [ ] **步骤 7：Commit**

```bash
git add frontend-next/app/[locale]/courses/[slug]/page.tsx frontend-next/app/[locale]/news/[slug]/page.tsx frontend-next/app/[locale]/campuses/[slug]/page.tsx frontend-next/app/[locale]/teachers/[slug]/page.tsx frontend-next/app/[locale]/faq/page.tsx
git commit -m "feat(seo): inject schema in detail pages (courses, news, campuses, teachers, faq)

- courses/[slug]: replace inline Course with buildCourseSchema + BreadcrumbList
- news/[slug]: replace inline NewsArticle with buildNewsArticleSchema + BreadcrumbList
- campuses/[slug]: add LocalBusiness + BreadcrumbList (new)
- teachers/[slug]: add Person + BreadcrumbList (new)
- faq: extract inline FAQPage to buildFaqPageSchema + add BreadcrumbList

Each detail page now has 2 JSON-LD scripts (entity + breadcrumb)."
```

---

## 任务 5：列表页/静态页/功能页 BreadcrumbList 注入

**文件：**
- 修改：`frontend-next/app/[locale]/courses/page.tsx`
- 修改：`frontend-next/app/[locale]/news/page.tsx`
- 修改：`frontend-next/app/[locale]/campuses/page.tsx`
- 修改：`frontend-next/app/[locale]/teachers/page.tsx`
- 修改：`frontend-next/app/[locale]/[slug]/page.tsx`
- 修改：`frontend-next/app/[locale]/refund-policy/page.tsx`
- 修改：`frontend-next/app/[locale]/privacy-policy/page.tsx`
- 修改：`frontend-next/app/[locale]/user-agreement/page.tsx`

**注意：** 先检查 `appointment/page.tsx` 和 `contact/page.tsx` 是否存在，如存在也需修改。

- [ ] **步骤 1：检查所有需修改的页面文件是否存在**

运行：
```bash
cd frontend-next && ls app/[locale]/{appointment,contact,refund-policy,privacy-policy,user-agreement}/page.tsx 2>/dev/null
```
记录哪些文件存在，对存在的文件都执行 BreadcrumbList 注入。

- [ ] **步骤 2：课程列表页 — 添加 BreadcrumbList**

在 `frontend-next/app/[locale]/courses/page.tsx` 中：

1. 添加导入（如尚无）：

```typescript
import { buildMetadata, buildJsonLd, buildBreadcrumbSchema } from '@/lib/seo';
```

2. 在 `generateMetadata` 之后的页面组件函数中，获取数据后、return 之前，添加：

```typescript
  const breadcrumbSchema = buildBreadcrumbSchema(
    [
      { name: locale === 'en-US' ? 'Home' : '首页', url: '/' },
      { name: locale === 'en-US' ? 'Courses' : '课程', url: '/courses' },
    ],
    locale as Locale
  );
```

3. 在 return 的最外层元素内部添加 script 标签：

```tsx
  return (
    <div ...>
      <script
        type="application/ld+json"
        dangerouslySetInnerHTML={{ __html: buildJsonLd(breadcrumbSchema) }}
      />
      {/* 原有内容 */}
```

- [ ] **步骤 3：新闻列表页 — 添加 BreadcrumbList**

对 `frontend-next/app/[locale]/news/page.tsx` 重复步骤 2 的模式，breadcrumb 数据：

```typescript
  const breadcrumbSchema = buildBreadcrumbSchema(
    [
      { name: locale === 'en-US' ? 'Home' : '首页', url: '/' },
      { name: locale === 'en-US' ? 'News' : '新闻动态', url: '/news' },
    ],
    locale as Locale
  );
```

- [ ] **步骤 4：校区列表页 — 添加 BreadcrumbList**

对 `frontend-next/app/[locale]/campuses/page.tsx` 重复步骤 2 的模式，breadcrumb 数据：

```typescript
  const breadcrumbSchema = buildBreadcrumbSchema(
    [
      { name: locale === 'en-US' ? 'Home' : '首页', url: '/' },
      { name: locale === 'en-US' ? 'Campuses' : '校区分布', url: '/campuses' },
    ],
    locale as Locale
  );
```

- [ ] **步骤 5：教师列表页 — 添加 BreadcrumbList**

对 `frontend-next/app/[locale]/teachers/page.tsx` 重复步骤 2 的模式，breadcrumb 数据：

```typescript
  const breadcrumbSchema = buildBreadcrumbSchema(
    [
      { name: locale === 'en-US' ? 'Home' : '首页', url: '/' },
      { name: locale === 'en-US' ? 'Teachers' : '师资团队', url: '/teachers' },
    ],
    locale as Locale
  );
```

- [ ] **步骤 6：静态单页 [slug]/page.tsx — 添加 BreadcrumbList**

在 `frontend-next/app/[locale]/[slug]/page.tsx` 中，重复步骤 2 的模式，breadcrumb 数据（使用页面 title 作为最后一级）：

```typescript
  const breadcrumbSchema = buildBreadcrumbSchema(
    [
      { name: locale === 'en-US' ? 'Home' : '首页', url: '/' },
      { name: page.title, url: `/${page.slug}` },
    ],
    locale as Locale
  );
```

- [ ] **步骤 7：政策页面 — 添加 BreadcrumbList**

对以下 3 个文件重复步骤 2 的模式：
- `frontend-next/app/[locale]/refund-policy/page.tsx`（breadcrumb: 首页 > 退费政策）
- `frontend-next/app/[locale]/privacy-policy/page.tsx`（breadcrumb: 首页 > 隐私政策）
- `frontend-next/app/[locale]/user-agreement/page.tsx`（breadcrumb: 首页 > 用户协议）

示例（refund-policy）：

```typescript
  const breadcrumbSchema = buildBreadcrumbSchema(
    [
      { name: locale === 'en-US' ? 'Home' : '首页', url: '/' },
      { name: locale === 'en-US' ? 'Refund Policy' : '退费政策', url: '/refund-policy' },
    ],
    locale as Locale
  );
```

- [ ] **步骤 8：功能页面（appointment、contact）— 添加 BreadcrumbList（如存在）**

对 `frontend-next/app/[locale]/appointment/page.tsx` 和 `frontend-next/app/[locale]/contact/page.tsx` 重复步骤 2 的模式（如文件存在）。

appointment breadcrumb：首页 > 预约试听
contact breadcrumb：首页 > 联系我们

- [ ] **步骤 9：运行测试验证无回归**

运行：
```bash
cd frontend-next && npx vitest run
```
预期：PASS，所有现有测试通过

- [ ] **步骤 10：Commit**

```bash
git add frontend-next/app/[locale]/courses/page.tsx frontend-next/app/[locale]/news/page.tsx frontend-next/app/[locale]/campuses/page.tsx frontend-next/app/[locale]/teachers/page.tsx frontend-next/app/[locale]/[slug]/page.tsx frontend-next/app/[locale]/refund-policy/page.tsx frontend-next/app/[locale]/privacy-policy/page.tsx frontend-next/app/[locale]/user-agreement/page.tsx
# 如存在也 add：
# frontend-next/app/[locale]/appointment/page.tsx frontend-next/app/[locale]/contact/page.tsx
git commit -m "feat(seo): add BreadcrumbList schema to list, static, and functional pages

- List pages: courses, news, campuses, teachers
- Static pages: [slug] (about us etc.)
- Policy pages: refund-policy, privacy-policy, user-agreement
- Functional pages: appointment, contact (if exist)

Each page gets a BreadcrumbList JSON-LD with locale-aware URLs."
```

---

## 任务 6：Sitemap 补全

**文件：**
- 修改：`frontend-next/app/sitemap.ts`
- 测试：`frontend-next/lib/__tests__/sitemap.test.ts`（新增）

- [ ] **步骤 1：编写 sitemap 测试**

创建 `frontend-next/lib/__tests__/sitemap.test.ts`：

```typescript
import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest';

describe('sitemap dynamic URLs', () => {
  beforeEach(() => {
    process.env.NEXT_PUBLIC_SITE_URL = 'https://example.com';
    vi.mock('@/lib/api', () => ({
      getProducts: vi.fn().mockResolvedValue({
        data: [{ slug: 'pinyin', updatedAt: '2026-01-01T00:00:00Z' }],
      }),
      getNews: vi.fn().mockResolvedValue({
        data: [{ slug: 'opening-notice', publishedAt: '2026-01-15T00:00:00Z' }],
      }),
      getCampuses: vi.fn().mockResolvedValue({
        data: [{ slug: 'baibuting' }],
      }),
      getTeachers: vi.fn().mockResolvedValue({
        data: [{ slug: 'zhang-laoshi' }],
      }),
    }));
  });

  afterEach(() => {
    delete process.env.NEXT_PUBLIC_SITE_URL;
    vi.restoreAllMocks();
  });

  it('includes campuses/[slug] dynamic URLs with hreflang', async () => {
    const { default: sitemap } = await import('../../app/sitemap');
    const entries = await sitemap();
    const campusUrls = entries.filter((e) => e.url.includes('/campuses/baibuting'));
    expect(campusUrls).toHaveLength(2);
    const zhEntry = campusUrls.find((e) => !e.url.includes('/en-US'));
    const enEntry = campusUrls.find((e) => e.url.includes('/en-US'));
    expect(zhEntry?.url).toBe('https://example.com/campuses/baibuting');
    expect(enEntry?.url).toBe('https://example.com/en-US/campuses/baibuting');
    expect(zhEntry?.alternates?.languages).toEqual({
      'zh-CN': 'https://example.com/campuses/baibuting',
      'en-US': 'https://example.com/en-US/campuses/baibuting',
    });
  });

  it('includes teachers/[slug] dynamic URLs with hreflang', async () => {
    const { default: sitemap } = await import('../../app/sitemap');
    const entries = await sitemap();
    const teacherUrls = entries.filter((e) => e.url.includes('/teachers/zhang-laoshi'));
    expect(teacherUrls).toHaveLength(2);
    const zhEntry = teacherUrls.find((e) => !e.url.includes('/en-US'));
    const enEntry = teacherUrls.find((e) => e.url.includes('/en-US'));
    expect(zhEntry?.url).toBe('https://example.com/teachers/zhang-laoshi');
    expect(enEntry?.url).toBe('https://example.com/en-US/teachers/zhang-laoshi');
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：
```bash
cd frontend-next && npx vitest run lib/__tests__/sitemap.test.ts
```
预期：FAIL，报错 campuses/teachers URL 不存在

- [ ] **步骤 3：实现 sitemap 补全**

在 `frontend-next/app/sitemap.ts` 中：

1. 更新导入（第 2 行）：

```typescript
import { getProducts, getNews, getCampuses, getTeachers } from '@/lib/api';
```

2. 在 news forEach 之后（约第 67 行之后），追加 campuses 和 teachers 动态 URL：

```typescript
  const { data: campuses } = await getCampuses().catch(() => ({ data: [] as never[] }));
  campuses.forEach((c) => {
    const zhUrl = `${baseUrl}/campuses/${c.slug}`;
    const enUrl = `${baseUrl}/en-US/campuses/${c.slug}`;
    [zhUrl, enUrl].forEach((url) => {
      entries.push({
        url,
        lastModified: new Date(),
        priority: 0.7,
        alternates: { languages: { 'zh-CN': zhUrl, 'en-US': enUrl } },
      });
    });
  });

  const { data: teachers } = await getTeachers().catch(() => ({ data: [] as never[] }));
  teachers.forEach((t) => {
    const zhUrl = `${baseUrl}/teachers/${t.slug}`;
    const enUrl = `${baseUrl}/en-US/teachers/${t.slug}`;
    [zhUrl, enUrl].forEach((url) => {
      entries.push({
        url,
        lastModified: new Date(),
        priority: 0.6,
        alternates: { languages: { 'zh-CN': zhUrl, 'en-US': enUrl } },
      });
    });
  });
```

- [ ] **步骤 4：运行测试验证通过**

运行：
```bash
cd frontend-next && npx vitest run lib/__tests__/sitemap.test.ts
```
预期：PASS

- [ ] **步骤 5：运行全部测试验证无回归**

运行：
```bash
cd frontend-next && npx vitest run
```
预期：PASS

- [ ] **步骤 6：Commit**

```bash
git add frontend-next/app/sitemap.ts frontend-next/lib/__tests__/sitemap.test.ts
git commit -m "feat(seo): add campuses and teachers dynamic URLs to sitemap

- Add campuses/[slug] dynamic URLs with zh-CN + en-US hreflang alternates
- Add teachers/[slug] dynamic URLs with zh-CN + en-US hreflang alternates
- Import getCampuses and getTeachers in sitemap.ts
- Unit tests verify URL format and hreflang structure"
```

---

## 任务 7：遗留清理

**文件：**
- 删除：`frontend-next/components/Seo.tsx`
- 修改：`frontend-next/components/course/CourseSearchPanel.tsx`

- [ ] **步骤 1：从 CourseSearchPanel 移除 Seo 使用**

在 `frontend-next/components/course/CourseSearchPanel.tsx` 中：

1. 删除导入行（第 8 行）：

```typescript
// 删除：import Seo from '@/components/Seo';
```

2. 删除 JSX 中的 `<Seo>` 使用（第 35 行）：

```tsx
// 删除：<Seo title={title} description={description} />
```

3. 检查 `title` 和 `description` 变量是否在其他地方使用，如不再使用则一并删除其声明。

- [ ] **步骤 2：删除 components/Seo.tsx**

运行：
```bash
cd frontend-next && rm components/Seo.tsx
```

- [ ] **步骤 3：验证无其他文件引用 Seo 组件**

运行：
```bash
cd frontend-next && grep -r "from '@/components/Seo'" --include="*.tsx" --include="*.ts" . || echo "No references found"
```
预期：输出 "No references found"

- [ ] **步骤 4：运行测试验证无回归**

运行：
```bash
cd frontend-next && npx vitest run
```
预期：PASS

- [ ] **步骤 5：Commit**

```bash
git add frontend-next/components/course/CourseSearchPanel.tsx
git rm frontend-next/components/Seo.tsx
git commit -m "refactor(seo): remove legacy client-side Seo component

- Delete components/Seo.tsx (client-side SEO component)
- Remove Seo usage from CourseSearchPanel.tsx
- SEO is now fully server-side via buildMetadata + buildJsonLd generators
- Client-side dynamic title/description had no SEO value (search engines
  see server-rendered HTML)"
```

---

## 任务 8：E2E 测试

**文件：**
- 创建：`frontend-next/e2e/seo.spec.ts`

- [ ] **步骤 1：编写 E2E 测试**

创建 `frontend-next/e2e/seo.spec.ts`：

```typescript
import { test, expect } from '@playwright/test';

test.describe('SEO structured data', () => {
  test('homepage has Organization + WebSite schema', async ({ page }) => {
    await page.goto('/');
    const scripts = page.locator('script[type="application/ld+json"]');
    const count = await scripts.count();
    expect(count).toBeGreaterThanOrEqual(2);

    const jsonContents = await scripts.allTextContents();
    const schemas = jsonContents.map((s) => JSON.parse(s));
    const types = schemas.map((s) => s['@type']);

    expect(types).toContain('WebSite');
    expect(types).toContain('EducationalOrganization');
  });

  test('course detail page has Course + BreadcrumbList schema', async ({ page }) => {
    await page.goto('/courses/pinyin');
    const scripts = page.locator('script[type="application/ld+json"]');
    const count = await scripts.count();
    expect(count).toBeGreaterThanOrEqual(2);

    const jsonContents = await scripts.allTextContents();
    const schemas = jsonContents.map((s) => JSON.parse(s));
    const types = schemas.map((s) => s['@type']);

    expect(types).toContain('Course');
    expect(types).toContain('BreadcrumbList');
  });

  test('campus detail page has LocalBusiness + BreadcrumbList schema', async ({ page }) => {
    await page.goto('/campuses/yousen-baibuting');
    const scripts = page.locator('script[type="application/ld+json"]');
    const count = await scripts.count();
    expect(count).toBeGreaterThanOrEqual(2);

    const jsonContents = await scripts.allTextContents();
    const schemas = jsonContents.map((s) => JSON.parse(s));

    const localBusiness = schemas.find((s) => {
      const t = s['@type'];
      return Array.isArray(t) ? t.includes('LocalBusiness') : t === 'LocalBusiness';
    });
    expect(localBusiness).toBeDefined();
    expect(localBusiness?.['@type']).toEqual(['LocalBusiness', 'EducationalOrganization']);
    expect(localBusiness?.['address']?.streetAddress).toBeTruthy();
    expect(localBusiness?.['name']).toBeTruthy();

    const breadcrumb = schemas.find((s) => s['@type'] === 'BreadcrumbList');
    expect(breadcrumb).toBeDefined();
  });

  test('FAQ page has FAQPage + BreadcrumbList schema', async ({ page }) => {
    await page.goto('/faq');
    const scripts = page.locator('script[type="application/ld+json"]');
    const count = await scripts.count();
    expect(count).toBeGreaterThanOrEqual(2);

    const jsonContents = await scripts.allTextContents();
    const schemas = jsonContents.map((s) => JSON.parse(s));
    const types = schemas.map((s) => s['@type']);

    expect(types).toContain('FAQPage');
    expect(types).toContain('BreadcrumbList');
  });

  test('sitemap.xml contains campuses and teachers URLs', async ({ page }) => {
    const response = await page.goto('/sitemap.xml');
    expect(response?.status()).toBe(200);
    const content = await response?.text();
    expect(content).toContain('/campuses/');
    expect(content).toContain('/teachers/');
    expect(content).toContain('hreflang');
  });
});
```

- [ ] **步骤 2：运行 E2E 测试（需运行中的 Next.js + Strapi）**

运行：
```bash
cd frontend-next && npx playwright test e2e/seo.spec.ts
```
预期：5 个测试通过（如服务未运行则跳过，记为已知限制）

**注意：** E2E 测试需要运行中的 Next.js dev server 和 Strapi 后端。如环境不可用，此步骤可跳过，记为已知限制。

- [ ] **步骤 3：Commit**

```bash
git add frontend-next/e2e/seo.spec.ts
git commit -m "test(seo): add E2E tests for JSON-LD structured data and sitemap

5 E2E test cases:
- Homepage: verify Organization + WebSite schema present
- Course detail: verify Course + BreadcrumbList schema
- Campus detail: verify LocalBusiness (dual @type) + BreadcrumbList
- FAQ page: verify FAQPage + BreadcrumbList
- Sitemap: verify campuses/teachers URLs with hreflang"
```

---

## 自检

**1. 规格覆盖度：**

| 规格章节 | 对应任务 |
|---------|---------|
| 2.2 buildOrganizationSchema | 任务 1 |
| 2.2 buildLocalBusinessSchema | 任务 2 |
| 2.2 buildBreadcrumbSchema | 任务 1 |
| 2.2 buildCourseSchema | 任务 2 |
| 2.2 buildNewsArticleSchema | 任务 2 |
| 2.2 buildWebSiteSchema | 任务 1 |
| 2.2 buildPersonSchema | 任务 2 |
| 2.2 buildFaqPageSchema | 任务 1 |
| 3.1 全局 Schema（layout.tsx） | 任务 3 |
| 3.2 各页面 Schema 注入 | 任务 4（详情页）+ 任务 5（列表/静态/功能页） |
| 4. Sitemap 补全 | 任务 6 |
| 5. 遗留清理 | 任务 7 |
| 6.2 E2E 测试 | 任务 8 |

**覆盖完整。** 无遗漏。

**2. 占位符扫描：** 已扫描，无 TODO/待定/模糊描述。所有代码步骤均含完整代码块。

**3. 类型一致性：**
- `buildBreadcrumbSchema` 签名在任务 1 定义为 `(items, locale)`，任务 4/5 调用均传 `(items, locale)` ✓
- `buildOrganizationSchema` 签名在任务 1 定义为 `(settings, socialLinks, locale)`，任务 3 调用传 `(settings, footer?.socialLinks || [], locale)` ✓
- `buildCourseSchema` 签名在任务 2 定义为 `(product, settings, locale)`，任务 4 调用传 `(product, settings || { name: '佑森小课堂' }, locale)` ✓
- `buildLocalBusinessSchema` 签名在任务 2 定义为 `(campus, locale)`，任务 4 调用传 `(campus, locale)` ✓
- `buildPersonSchema` 签名在任务 2 定义为 `(teacher, locale)`，任务 4 调用传 `(teacher, locale)` ✓
- `buildNewsArticleSchema` 签名在任务 2 定义为 `(news, settings, locale)`，任务 4 调用传 `(news, newsSettings, locale)` ✓
- `buildFaqPageSchema` 签名在任务 1 定义为 `(faqItems)`，任务 4 调用传 `(faqItems)` ✓
- `buildWebSiteSchema` 签名在任务 1 定义为 `(settings, locale)`，任务 3 调用传 `(settings, locale)` ✓

**类型一致。** 无矛盾。

---

## 执行交接

计划已完成并保存到 `docs/superpowers/plans/2026-07-15-seo-geo.md`。两种执行方式：

**1. 子代理驱动（推荐）** - 每个任务调度一个新的子代理，任务间进行审查，快速迭代

**2. 内联执行** - 在当前会话中使用 executing-plans 执行任务，批量执行并设有检查点

选哪种方式？
