# 子项目 5A：多语言 i18n 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 为佑森小课堂网站实现中英双语支持，包括 next-intl 路由、Strapi i18n 内容获取、AI 客服双语、翻译辅助 API、hreflang SEO 标签。

**架构：** Next.js App Router `[locale]` 动态段 + next-intl 中间件（`localePrefix: 'as-needed'`，中文无前缀、英文 `/en-US/`）；UI 文案走 `messages/*.json`，CMS 内容走 Strapi i18n API；AI 客服通过 `session.locale` + 知识库 `locale` 字段实现双语；翻译辅助 API 复用 DashScope。

**技术栈：** next-intl ^3.x、Strapi v5 i18n plugin、PostgreSQL + pgvector、DashScope qwen-plus、Jest + Playwright。

**规格文档：** `docs/superpowers/specs/2026-07-14-i18n-design.md`

---

## 文件结构

### 后端新增/修改文件

| 文件 | 操作 | 职责 |
|------|------|------|
| `backend/scripts/migrate-knowledge-base-locale.ts` | 创建 | 迁移脚本：为 knowledge_bases 表已有数据补 locale='zh-CN' |
| `backend/src/services/ai-config-service.ts` | 修改 | AiConfig 接口新增 `systemPromptEn` 字段 + 读取逻辑 |
| `backend/src/services/rag-service.ts` | 修改 | `retrieve()` 接受 locale 参数，SQL 追加 locale 过滤 + fallback 逻辑 |
| `backend/src/api/chat/controllers/chat.ts` | 修改 | `startSession` 接受 locale，`sendMessage` 用 session.locale 覆盖入参 |
| `backend/src/services/knowledge-sync-service.ts` | 修改 | `syncSingleContent` 按 locale 写入，`deleteSyncedContent` 按 documentId+locale 删除 |
| `backend/src/api/translation/controllers/translation.ts` | 创建 | 翻译辅助 API controller |
| `backend/src/api/translation/routes/translation.ts` | 创建 | 翻译辅助 API 路由（POST /api/translation/assist，admin only） |
| `backend/src/api/translation/services/translation.ts` | 创建 | 翻译辅助 service（DashScope 调用 + 字段校验） |

### 后端测试文件

| 文件 | 操作 |
|------|------|
| `backend/scripts/__tests__/migrate-knowledge-base-locale.test.ts` | 创建 |
| `backend/src/api/translation/controllers/__tests__/translation.test.ts` | 创建 |
| `backend/src/api/chat/controllers/__tests__/chat.test.ts` | 修改（扩展 locale 用例） |
| `backend/src/services/__tests__/rag-service.test.ts` | 创建（locale 过滤 + fallback） |
| `backend/src/services/__tests__/knowledge-sync-service.test.ts` | 修改（扩展 locale 用例） |

### 前端新增/修改文件

| 文件 | 操作 | 职责 |
|------|------|------|
| `frontend-next/i18n/routing.ts` | 创建 | defineRouting（locales、defaultLocale、localePrefix） |
| `frontend-next/i18n/request.ts` | 创建 | getRequestConfig（加载 messages + onError 降级） |
| `frontend-next/i18n/messages/zh-CN.json` | 创建 | UI 文案中文 |
| `frontend-next/i18n/messages/en-US.json` | 创建 | UI 文案英文 |
| `frontend-next/i18n/messages/__tests__/messages.test.ts` | 创建 | 双向 key 完整性测试 |
| `frontend-next/middleware.ts` | 创建 | next-intl createMiddleware |
| `frontend-next/next.config.ts` | 修改 | 增加 next-intl/plugin |
| `frontend-next/app/layout.tsx` | 修改 | 改为最小壳层 |
| `frontend-next/app/[locale]/layout.tsx` | 创建 | 由原 layout.tsx 迁入，接收 params.locale |
| `frontend-next/app/[locale]/*` | 创建 | 由 app/* 迁入所有页面 |
| `frontend-next/lib/api.ts` | 修改 | 所有函数加 locale 参数 + detail 页 fallback |
| `frontend-next/lib/seo.ts` | 修改 | buildMetadata 注入 hreflang |
| `frontend-next/components/layout/LanguageSwitcher.tsx` | 创建 | 语言切换器 |
| `frontend-next/components/layout/Navigation.tsx` | 修改 | 插入 LanguageSwitcher |
| `frontend-next/components/chat/FloatingChat.tsx` | 修改 | 接受 locale prop，传给 startChat |
| `frontend-next/lib/chat.ts` | 修改 | startChat/sendMessage 接受 locale |
| `frontend-next/app/sitemap.ts` | 修改 | 输出 hreflang 双 URL |
| `frontend-next/app/llms.txt/route.ts` | 修改 | 输出双语内容 |

### 前端测试文件

| 文件 | 操作 |
|------|------|
| `frontend-next/lib/__tests__/api.test.ts` | 修改（扩展 locale 用例） |
| `frontend-next/lib/__tests__/seo.test.ts` | 修改（扩展 hreflang 用例） |
| `frontend-next/components/layout/__tests__/LanguageSwitcher.test.tsx` | 创建 |
| `frontend-next/e2e/i18n.spec.ts` | 创建 |
| `frontend-next/e2e/i18n-seo.spec.ts` | 创建 |
| `frontend-next/e2e/i18n-chat.spec.ts` | 创建 |
| `frontend-next/e2e/visual-i18n.spec.ts` | 创建 |

---

## 任务 1：knowledge_base 迁移脚本（先迁移后上线）

**文件：**
- 创建：`backend/scripts/migrate-knowledge-base-locale.ts`
- 创建：`backend/scripts/__tests__/migrate-knowledge-base-locale.test.ts`

**前提说明：** knowledge_bases 表需要新增 `locale` 字段（VARCHAR(10) NOT NULL DEFAULT 'zh-CN'）。该字段通过 Strapi Content-Type Builder 在 admin 界面手动添加（schema 变更不在代码任务范围内），本任务假设字段已添加，编写迁移脚本为已有 16 条记录补齐 locale 值。

- [ ] **步骤 1：编写失败的迁移脚本测试**

创建 `backend/scripts/__tests__/migrate-knowledge-base-locale.test.ts`：

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';

vi.mock('@strapi/strapi', () => ({
  createStrapi: vi.fn(),
}));

describe('migrate-knowledge-base-locale', () => {
  let mockRaw: ReturnType<typeof vi.fn>;
  let mockStrapi: any;

  beforeEach(() => {
    mockRaw = vi.fn();
    mockStrapi = {
      db: { connection: { raw: mockRaw } },
      destroy: vi.fn(),
    };
    vi.doMock('@strapi/strapi', () => ({ createStrapi: vi.fn().mockResolvedValue(mockStrapi) }));
  });

  it('updates NULL locale rows to zh-CN', async () => {
    mockRaw.mockResolvedValue({ rows: [{ updated: 5 }] });
    const { migrateKnowledgeBaseLocale } = await import('../migrate-knowledge-base-locale');
    const result = await migrateKnowledgeBaseLocale(mockStrapi);
    expect(mockRaw).toHaveBeenCalledWith(
      "UPDATE knowledge_bases SET locale = 'zh-CN' WHERE locale IS NULL",
      []
    );
    expect(result.updated).toBe(5);
  });

  it('is idempotent — rows with locale are not overwritten', async () => {
    mockRaw.mockResolvedValue({ rows: [{ updated: 0 }] });
    const { migrateKnowledgeBaseLocale } = await import('../migrate-knowledge-base-locale');
    const result = await migrateKnowledgeBaseLocale(mockStrapi);
    expect(result.updated).toBe(0);
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`cd backend && npx vitest run scripts/__tests__/migrate-knowledge-base-locale.test.ts`
预期：FAIL，报错 "Cannot find module '../migrate-knowledge-base-locale'"

- [ ] **步骤 3：编写迁移脚本实现**

创建 `backend/scripts/migrate-knowledge-base-locale.ts`：

```typescript
/**
 * Migration script: backfill the `locale` column for existing knowledge_bases rows.
 *
 * Usage (from backend/):
 *   npx tsx scripts/migrate-knowledge-base-locale.ts
 *
 * The `locale` column (VARCHAR(10) NOT NULL DEFAULT 'zh-CN') is added via
 * Strapi Content-Type Builder before running this script. Existing rows
 * inserted before the column existed will have NULL locale despite the
 * DEFAULT clause — this script sets them all to 'zh-CN'.
 */
export async function migrateKnowledgeBaseLocale(strapi: any): Promise<{ updated: number }> {
  const result = await strapi.db.connection.raw(
    "UPDATE knowledge_bases SET locale = 'zh-CN' WHERE locale IS NULL",
    []
  );

  // PostgreSQL returns { rowCount } or { rows: [{ updated: N }] } depending on
  // the Knex version; handle both shapes.
  const updated =
    (result as any).rowCount ??
    (result as any).rows?.[0]?.updated ??
    0;

  console.log(`[migrate-knowledge-base-locale] Updated ${updated} rows to locale='zh-CN'`);
  return { updated };
}

// CLI entry point
async function main() {
  const { createStrapi } = await import('@strapi/strapi');
  const strapi = await createStrapi().load();
  try {
    await migrateKnowledgeBaseLocale(strapi);
  } finally {
    await strapi.destroy();
  }
}

if (require.main === module) {
  main().catch((err) => {
    console.error(err);
    process.exit(1);
  });
}
```

- [ ] **步骤 4：运行测试验证通过**

运行：`cd backend && npx vitest run scripts/__tests__/migrate-knowledge-base-locale.test.ts`
预期：PASS（2 个用例）

- [ ] **步骤 5：Commit**

```bash
cd backend
git add scripts/migrate-knowledge-base-locale.ts scripts/__tests__/migrate-knowledge-base-locale.test.ts
git commit -m "feat(backend): knowledge_base locale 迁移脚本

为已有 knowledge_bases 记录补齐 locale='zh-CN'，幂等执行。
先迁移后上线，确保后续 locale 过滤代码不踩 NULL 坑。"
```

---

## 任务 2：ai-config 扩展 systemPromptEn 字段

**文件：**
- 修改：`backend/src/services/ai-config-service.ts`

**前提说明：** `ai_config` 表需要在 Strapi admin 通过 Content-Type Builder 新增 `systemPromptEn`（TEXT, 可空）字段。本任务假设字段已添加，修改 service 层读取逻辑。

- [ ] **步骤 1：编写失败的 service 测试**

创建 `backend/src/services/__tests__/ai-config-service.test.ts`：

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { getActiveAiConfig, clearCache } from '../ai-config-service';

describe('ai-config-service systemPromptEn', () => {
  beforeEach(() => {
    clearCache();
  });

  it('returns systemPromptEn from database when present', async () => {
    const mockStrapi = {
      documents: vi.fn().mockReturnValue({
        findMany: vi.fn().mockResolvedValue([
          {
            provider: 'qwen',
            model: 'qwen-plus',
            embeddingModel: 'text-embedding-v2',
            apiKey: 'sk-test',
            apiEndpoint: 'https://test',
            systemPrompt: '中文 prompt',
            systemPromptEn: 'English prompt',
            temperature: 0.3,
            maxTokens: 1000,
            topK: 5,
            chunkSize: 500,
            chunkOverlap: 50,
          },
        ]),
      }),
    } as any;

    const config = await getActiveAiConfig(mockStrapi);
    expect(config?.systemPromptEn).toBe('English prompt');
    expect(config?.systemPrompt).toBe('中文 prompt');
  });

  it('returns null systemPromptEn when database field is empty', async () => {
    const mockStrapi = {
      documents: vi.fn().mockReturnValue({
        findMany: vi.fn().mockResolvedValue([
          {
            provider: 'qwen',
            model: 'qwen-plus',
            embeddingModel: 'text-embedding-v2',
            apiKey: 'sk-test',
            apiEndpoint: 'https://test',
            systemPrompt: '中文 prompt',
            systemPromptEn: null,
            temperature: 0.3,
            maxTokens: 1000,
            topK: 5,
            chunkSize: 500,
            chunkOverlap: 50,
          },
        ]),
      }),
    } as any;

    const config = await getActiveAiConfig(mockStrapi);
    expect(config?.systemPromptEn).toBeNull();
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`cd backend && npx vitest run src/services/__tests__/ai-config-service.test.ts`
预期：FAIL，报错 "Property 'systemPromptEn' does not exist on type 'AiConfig'"

- [ ] **步骤 3：修改 AiConfig 接口和读取逻辑**

修改 `backend/src/services/ai-config-service.ts`：

```typescript
export interface AiConfig {
  provider: 'qwen' | 'openai' | 'custom';
  model: string;
  embeddingModel: string;
  apiKey: string;
  apiEndpoint: string;
  systemPrompt: string;
  systemPromptEn: string | null;  // 新增
  temperature: number;
  maxTokens: number;
  topK: number;
  chunkSize: number;
  chunkOverlap: number;
}

// 在 getActiveAiConfig 函数内，构造 aiConfig 对象处新增：
const aiConfig: AiConfig = {
  provider: config.provider,
  model: config.model,
  embeddingModel: config.embeddingModel,
  apiKey: config.apiKey,
  apiEndpoint: config.apiEndpoint,
  systemPrompt: config.systemPrompt,
  systemPromptEn: config.systemPromptEn ?? null,  // 新增
  temperature: config.temperature,
  maxTokens: config.maxTokens,
  topK: config.topK,
  chunkSize: config.chunkSize,
  chunkOverlap: config.chunkOverlap,
};
```

- [ ] **步骤 4：运行测试验证通过**

运行：`cd backend && npx vitest run src/services/__tests__/ai-config-service.test.ts`
预期：PASS（2 个用例）

- [ ] **步骤 5：Commit**

```bash
cd backend
git add src/services/ai-config-service.ts src/services/__tests__/ai-config-service.test.ts
git commit -m "feat(backend): ai-config 新增 systemPromptEn 字段

与现有 systemPrompt（中文）并列，为空时 fallback 到环境变量。
管理员可在后台分别配置中英文系统提示词。"
```

---

## 任务 3：rag-service locale 过滤 + fallback

**文件：**
- 修改：`backend/src/services/rag-service.ts`
- 创建：`backend/src/services/__tests__/rag-service.test.ts`

- [ ] **步骤 1：编写失败的 rag-service 测试**

创建 `backend/src/services/__tests__/rag-service.test.ts`：

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { retrieve, setStrapi } from '../rag-service';

describe('rag-service retrieve with locale', () => {
  let mockStrapi: any;
  let mockRaw: ReturnType<typeof vi.fn>;

  beforeEach(() => {
    mockRaw = vi.fn();
    mockStrapi = {
      db: { connection: { raw: mockRaw } },
    };
    setStrapi(mockStrapi);

    // Mock generateEmbedding via the llm-service module
    vi.mock('../../../src/services/llm-service', () => ({
      generateEmbedding: vi.fn().mockResolvedValue({ embedding: [0.1, 0.2, 0.3] }),
      chat: vi.fn(),
    }));
  });

  it('queries with locale filter when locale=en-US and returns en-US docs', async () => {
    mockRaw.mockResolvedValue({
      rows: [
        { id: 1, chunk_text: 'English content', knowledge_base_id: 10, similarity: 0.85, locale: 'en-US' },
      ],
    });

    const result = await retrieve('hello', 5, 'en-US');
    expect(mockRaw).toHaveBeenCalledTimes(1);
    const sql = mockRaw.mock.calls[0][0] as string;
    expect(sql).toContain("kb.locale = 'en-US'");
    expect(result.docs).toHaveLength(1);
    expect(result.isRelevant).toBe(true);
  });

  it('falls back to zh-CN when en-US returns fewer than 2 docs', async () => {
    // First call (en-US) returns 1 doc; second call (zh-CN) returns 3 docs
    mockRaw
      .mockResolvedValueOnce({
        rows: [
          { id: 1, chunk_text: 'English content', knowledge_base_id: 10, similarity: 0.85, locale: 'en-US' },
        ],
      })
      .mockResolvedValueOnce({
        rows: [
          { id: 2, chunk_text: '中文内容1', knowledge_base_id: 11, similarity: 0.9, locale: 'zh-CN' },
          { id: 3, chunk_text: '中文内容2', knowledge_base_id: 12, similarity: 0.8, locale: 'zh-CN' },
          { id: 4, chunk_text: '中文内容3', knowledge_base_id: 13, similarity: 0.75, locale: 'zh-CN' },
        ],
      });

    const result = await retrieve('hello', 5, 'en-US');
    expect(mockRaw).toHaveBeenCalledTimes(2);
    const firstSql = mockRaw.mock.calls[0][0] as string;
    const secondSql = mockRaw.mock.calls[1][0] as string;
    expect(firstSql).toContain("kb.locale = 'en-US'");
    expect(secondSql).toContain("kb.locale = 'zh-CN'");
    // Merged result: 1 en-US + 3 zh-CN = 4 docs, with fallback flag
    expect(result.docs).toHaveLength(4);
    expect(result.usedFallback).toBe(true);
  });

  it('does not fallback when locale=zh-CN (default)', async () => {
    mockRaw.mockResolvedValue({ rows: [] });

    const result = await retrieve('hello', 5, 'zh-CN');
    expect(mockRaw).toHaveBeenCalledTimes(1);
    expect(result.docs).toHaveLength(0);
    expect(result.isRelevant).toBe(false);
    expect(result.usedFallback).toBe(false);
  });

  it('defaults to zh-CN when locale not provided', async () => {
    mockRaw.mockResolvedValue({ rows: [] });

    await retrieve('hello', 5);
    const sql = mockRaw.mock.calls[0][0] as string;
    expect(sql).toContain("kb.locale = 'zh-CN'");
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`cd backend && npx vitest run src/services/__tests__/rag-service.test.ts`
预期：FAIL，报错 "Property 'usedFallback' does not exist on type 'RetrievalResult'" + retrieve 签名不匹配

- [ ] **步骤 3：修改 rag-service retrieve 函数**

修改 `backend/src/services/rag-service.ts`，替换 `retrieve` 函数：

```typescript
export interface RetrievalResult {
  docs: RetrievedDoc[];
  isRelevant: boolean;
  usedFallback: boolean;  // 新增
}

/**
 * Retrieve relevant knowledge-base chunks by locale.
 *
 * - When locale='en-US' and en-US hits < 2, automatically fallback to zh-CN
 *   and merge results. `usedFallback=true` indicates the merge happened.
 * - When locale='zh-CN' (default), no fallback.
 */
export async function retrieve(
  query: string,
  topK = 5,
  locale: 'zh-CN' | 'en-US' = 'zh-CN'
): Promise<RetrievalResult> {
  if (!query || query.trim().length === 0) {
    return { docs: [], isRelevant: false, usedFallback: false };
  }

  if (!strapiInstance) {
    throw new Error('Strapi instance not initialized. Call setStrapi() first.');
  }

  const { embedding } = await generateEmbedding(query);
  if (!embedding || embedding.length === 0) {
    return { docs: [], isRelevant: false, usedFallback: false };
  }

  const embeddingJson = JSON.stringify(embedding);

  const buildSql = (loc: string) =>
    `SELECT ke.id, ke.chunk_text, ke.knowledge_base_id, kb.locale, 1 - (ke.embedding <=> ?::vector) as similarity
     FROM knowledge_embeddings ke
     JOIN knowledge_bases kb ON ke.knowledge_base_id = kb.id
     WHERE kb.status = 'ready' AND kb.locale = '${loc}'
     ORDER BY ke.embedding <=> ?::vector
     LIMIT ?`;

  const fetchByLocale = async (loc: string): Promise<RetrievedDoc[]> => {
    const result = await strapiInstance.db.connection.raw(buildSql(loc), [
      embeddingJson,
      embeddingJson,
      topK,
    ]);
    const rows: Array<Record<string, unknown>> = Array.isArray(result)
      ? result
      : result.rows || [];
    return rows
      .map((row) => ({
        id: Number(row.id),
        chunkText: String(row.chunk_text ?? ''),
        knowledgeBaseId: Number(row.knowledge_base_id),
        similarity: Number(row.similarity),
      }))
      .filter((doc) => doc.similarity >= SIMILARITY_THRESHOLD);
  };

  // Primary locale query
  const primaryDocs = await fetchByLocale(locale);

  // Fallback: en-US with < 2 hits → merge zh-CN results
  if (locale === 'en-US' && primaryDocs.length < 2) {
    const fallbackDocs = await fetchByLocale('zh-CN');
    const merged = [...primaryDocs, ...fallbackDocs].slice(0, topK);
    return {
      docs: merged,
      isRelevant: merged.length > 0,
      usedFallback: fallbackDocs.length > 0,
    };
  }

  return {
    docs: primaryDocs,
    isRelevant: primaryDocs.length > 0,
    usedFallback: false,
  };
}
```

- [ ] **步骤 4：运行测试验证通过**

运行：`cd backend && npx vitest run src/services/__tests__/rag-service.test.ts`
预期：PASS（4 个用例）

- [ ] **步骤 5：Commit**

```bash
cd backend
git add src/services/rag-service.ts src/services/__tests__/rag-service.test.ts
git commit -m "feat(backend): rag-service 支持 locale 过滤 + en-US fallback

retrieve(query, topK, locale) 按 locale 过滤知识库；
en-US 命中 < 2 时自动 fallback 合并 zh-CN 结果，usedFallback 标记。"
```

---

## 任务 4：chat controller 双语支持

**文件：**
- 修改：`backend/src/api/chat/controllers/chat.ts`
- 修改：`backend/src/api/chat/controllers/__tests__/chat.test.ts`

- [ ] **步骤 1：编写失败的 chat locale 测试**

在 `backend/src/api/chat/controllers/__tests__/chat.test.ts` 末尾追加：

```typescript
describe('chat startSession locale handling', () => {
  it('persists locale=en-US when provided', async () => {
    const ctx: any = {
      request: { body: { locale: 'en-US' } },
      body: null as any,
    };
    const mockCreate = vi.fn().mockResolvedValue({ documentId: 'doc1' });
    const mockStrapi = {
      documents: vi.fn().mockReturnValue({ create: mockCreate }),
    };
    vi.doMock('../../../../services/rag-service', () => ({}));
    vi.doMock('../../../../services/llm-service', () => ({}));
    vi.stubGlobal('strapi', mockStrapi);

    await chatController.startSession(ctx);
    expect(mockCreate).toHaveBeenCalledWith(
      expect.objectContaining({
        data: expect.objectContaining({ locale: 'en-US' }),
      })
    );
    expect(ctx.body.sessionId).toBeDefined();
  });

  it('defaults to locale=zh-CN when not provided', async () => {
    const ctx: any = {
      request: { body: {} },
      body: null as any,
    };
    const mockCreate = vi.fn().mockResolvedValue({ documentId: 'doc1' });
    const mockStrapi = {
      documents: vi.fn().mockReturnValue({ create: mockCreate }),
    };
    vi.stubGlobal('strapi', mockStrapi);

    await chatController.startSession(ctx);
    expect(mockCreate).toHaveBeenCalledWith(
      expect.objectContaining({
        data: expect.objectContaining({ locale: 'zh-CN' }),
      })
    );
  });

  it('defaults to zh-CN when locale is invalid', async () => {
    const ctx: any = {
      request: { body: { locale: 'fr-FR' } },
      body: null as any,
    };
    const mockCreate = vi.fn().mockResolvedValue({ documentId: 'doc1' });
    const mockStrapi = {
      documents: vi.fn().mockReturnValue({ create: mockCreate }),
    };
    vi.stubGlobal('strapi', mockStrapi);

    await chatController.startSession(ctx);
    expect(mockCreate).toHaveBeenCalledWith(
      expect.objectContaining({
        data: expect.objectContaining({ locale: 'zh-CN' }),
      })
    );
  });
});

describe('chat sendMessage locale override', () => {
  it('uses session.locale to override incoming locale param', async () => {
    // Session has locale=en-US; incoming body has locale=zh-CN (tamper attempt)
    const ctx: any = {
      request: { body: { sessionId: 'sess_1', message: 'hello', locale: 'zh-CN' } },
      body: null as any,
      throw: vi.fn(),
    };
    const mockFindMany = vi.fn().mockResolvedValue([
      { id: 1, documentId: 'doc1', sessionId: 'sess_1', status: 'active', messageCount: 0, locale: 'en-US' },
    ]);
    const mockStrapi = {
      documents: vi.fn().mockReturnValue({
        findMany: mockFindMany,
        create: vi.fn(),
      }),
      db: { query: vi.fn().mockReturnValue({ findMany: vi.fn().mockResolvedValue([]), update: vi.fn() }) },
    };
    vi.stubGlobal('strapi', mockStrapi);

    const mockRetrieve = vi.fn().mockResolvedValue({ docs: [], isRelevant: false, usedFallback: false });
    vi.doMock('../../../../services/rag-service', () => ({
      retrieve: mockRetrieve,
      generateAnswer: vi.fn(),
    }));
    vi.doMock('../../../../services/llm-service', () => ({ detectIntent: vi.fn().mockResolvedValue({ shouldTransfer: false }) }));

    await chatController.sendMessage(ctx);

    // retrieve should be called with 'en-US' (from session), not 'zh-CN' (from body)
    expect(mockRetrieve).toHaveBeenCalledWith('hello', 5, 'en-US');
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`cd backend && npx vitest run src/api/chat/controllers/__tests__/chat.test.ts`
预期：FAIL，startSession 不接受 locale，retrieve 调用未传 locale

- [ ] **步骤 3：修改 startSession 接受 locale**

修改 `backend/src/api/chat/controllers/chat.ts` 的 `startSession` 函数：

```typescript
const VALID_LOCALES = ['zh-CN', 'en-US'] as const;
type Locale = (typeof VALID_LOCALES)[number];

function normalizeLocale(input: unknown): Locale {
  if (typeof input === 'string' && (VALID_LOCALES as readonly string[]).includes(input)) {
    return input as Locale;
  }
  return 'zh-CN';
}

export default {
  async startSession(ctx: any) {
    const { visitorName, visitorPhone, sourcePage, locale } = ctx.request.body || {};

    // 基本输入校验
    if (visitorPhone !== undefined && visitorPhone !== null) {
      if (typeof visitorPhone !== 'string' || visitorPhone.length > 20) {
        ctx.throw(400, 'visitorPhone 格式不合法');
      }
    }
    if (visitorName !== undefined && visitorName !== null) {
      if (typeof visitorName !== 'string' || visitorName.length > 50) {
        ctx.throw(400, 'visitorName 格式不合法');
      }
    }

    const sessionLocale = normalizeLocale(locale);
    const sessionId = randomId('sess');
    const visitorId = randomId('vis');

    const session = await strapi.documents('api::chat-session.chat-session').create({
      data: {
        sessionId,
        visitorId,
        visitorName: visitorName || null,
        visitorPhone: visitorPhone || null,
        sourcePage: sourcePage || null,
        locale: sessionLocale,  // 新增
        status: 'active',
      },
    });

    ctx.body = { sessionId, visitorId, documentId: session.documentId };
  },

  // ... sendMessage 修改见下方
```

- [ ] **步骤 4：修改 sendMessage 用 session.locale 覆盖**

在 `sendMessage` 函数内，找到 `retrieve` 调用处，修改为传 session.locale：

```typescript
// 在 "const { docs, isRelevant } = await retrieve(message, 5);" 这行之前添加：
const sessionLocale = normalizeLocale(session.locale);

// 修改 retrieve 调用：
const { retrieve, generateAnswer } = require('../../../services/rag-service') as {
  retrieve: (query: string, topK: number, locale?: 'zh-CN' | 'en-US') => Promise<{ docs: any[]; isRelevant: boolean; usedFallback: boolean }>;
  generateAnswer: (
    query: string,
    docs: any[],
    history: ChatMessageRow[]
  ) => Promise<string>;
};

const { docs, isRelevant, usedFallback } = await retrieve(message, 5, sessionLocale);
```

同时修改 `buildSystemPrompt` 在 rag-service.ts 中：当 `usedFallback=true` 且 locale=en-US 时，在 prompt 中追加 "Answer in English. If context is Chinese, translate before answering."

修改 `backend/src/services/rag-service.ts` 的 `generateAnswer` 函数签名，接受 locale + usedFallback：

```typescript
export async function generateAnswer(
  query: string,
  docs: RetrievedDoc[],
  history: ChatMessageRow[],
  locale: 'zh-CN' | 'en-US' = 'zh-CN',
  usedFallback: boolean = false
): Promise<string> {
  const config = await getActiveAiConfig(strapiInstance);
  const systemPrompt = locale === 'en-US' && config?.systemPromptEn
    ? config.systemPromptEn
    : config?.systemPrompt || process.env.AI_CHAT_SYSTEM_PROMPT || SYSTEM_PROMPT_TEMPLATE;

  let finalPrompt = systemPrompt;
  if (locale === 'en-US' && usedFallback) {
    finalPrompt += '\n\nNote: Some context is in Chinese. Translate the answer to English.';
  }

  // ... 其余调用 chat() 的逻辑保持不变，使用 finalPrompt
}
```

对应地在 chat controller 的 sendMessage 中调用 generateAnswer 时传参：

```typescript
const answer = await generateAnswer(
  message,
  docs,
  (history || []).map((h: any) => ({ role: h.role, content: h.content })),
  sessionLocale,
  usedFallback
);
```

- [ ] **步骤 5：运行测试验证通过**

运行：`cd backend && npx vitest run src/api/chat/controllers/__tests__/chat.test.ts`
预期：PASS（所有用例包括新增 4 个）

- [ ] **步骤 6：Commit**

```bash
cd backend
git add src/api/chat/controllers/chat.ts src/api/chat/controllers/__tests__/chat.test.ts src/services/rag-service.ts
git commit -m "feat(backend): chat controller 双语支持

startSession 接受 locale 参数（默认 zh-CN，非法值回退）；
sendMessage 用 session.locale 覆盖入参（防篡改）；
retrieve 按 session.locale 过滤；
generateAnswer 按 locale 选 systemPromptEn/systemPrompt，fallback 时追加英文翻译指令。"
```

---

## 任务 5：knowledge-sync-service locale 支持

**文件：**
- 修改：`backend/src/services/knowledge-sync-service.ts`
- 修改：`backend/src/services/__tests__/knowledge-sync-service.test.ts`

- [ ] **步骤 1：编写失败的 sync locale 测试**

在 `backend/src/services/__tests__/knowledge-sync-service.test.ts` 追加：

```typescript
describe('syncSingleContent with locale', () => {
  it('writes locale=en-US to knowledge_base when record has locale=en-US', async () => {
    const mockFindOne = vi.fn().mockResolvedValue(null);
    const mockCreate = vi.fn().mockResolvedValue({});
    const mockStrapi = {
      db: { query: vi.fn().mockReturnValue({ findOne: mockFindOne }) },
      documents: vi.fn().mockReturnValue({ create: mockCreate }),
    } as any;

    await syncSingleContent(mockStrapi, 'api::course.course', {
      documentId: 'doc1',
      title: 'English Course',
      description: 'desc',
      locale: 'en-US',
    });

    expect(mockCreate).toHaveBeenCalledWith(
      expect.objectContaining({
        data: expect.objectContaining({ locale: 'en-US' }),
      })
    );
  });

  it('writes locale=zh-CN when record has no locale field', async () => {
    const mockFindOne = vi.fn().mockResolvedValue(null);
    const mockCreate = vi.fn().mockResolvedValue({});
    const mockStrapi = {
      db: { query: vi.fn().mockReturnValue({ findOne: mockFindOne }) },
      documents: vi.fn().mockReturnValue({ create: mockCreate }),
    } as any;

    await syncSingleContent(mockStrapi, 'api::course.course', {
      documentId: 'doc2',
      title: '中文课程',
    });

    expect(mockCreate).toHaveBeenCalledWith(
      expect.objectContaining({
        data: expect.objectContaining({ locale: 'zh-CN' }),
      })
    );
  });

  it('creates two independent records for same documentId different locales', async () => {
    // First sync (zh-CN) — no existing
    // Second sync (en-US) — no existing (because sourceUrl now includes locale)
    const mockFindOne = vi.fn().mockResolvedValue(null);
    const mockCreate = vi.fn().mockResolvedValue({});
    const mockStrapi = {
      db: { query: vi.fn().mockReturnValue({ findOne: mockFindOne }) },
      documents: vi.fn().mockReturnValue({ create: mockCreate }),
    } as any;

    await syncSingleContent(mockStrapi, 'api::course.course', {
      documentId: 'doc1',
      title: '中文课程',
      locale: 'zh-CN',
    });
    await syncSingleContent(mockStrapi, 'api::course.course', {
      documentId: 'doc1',
      title: 'English Course',
      locale: 'en-US',
    });

    expect(mockCreate).toHaveBeenCalledTimes(2);
    const firstCall = mockCreate.mock.calls[0][0].data;
    const secondCall = mockCreate.mock.calls[1][0].data;
    expect(firstCall.locale).toBe('zh-CN');
    expect(secondCall.locale).toBe('en-US');
    // sourceUrl should differ by locale
    expect(firstCall.sourceUrl).not.toBe(secondCall.sourceUrl);
  });

  it('deleteSyncedContent only deletes matching documentId + locale', async () => {
    const mockFindOne = vi.fn().mockResolvedValue({ id: 5, documentId: 'kb1' });
    const mockDeleteVectors = vi.fn();
    const mockDelete = vi.fn();
    const mockStrapi = {
      db: { query: vi.fn().mockReturnValue({ findOne: mockFindOne }) },
      documents: vi.fn().mockReturnValue({ delete: mockDelete }),
      service: vi.fn().mockReturnValue({ deleteVectors: mockDeleteVectors }),
    } as any;

    await deleteSyncedContent(mockStrapi, 'api::course.course', {
      documentId: 'doc1',
      locale: 'en-US',
    });

    expect(mockFindOne).toHaveBeenCalledWith(
      expect.objectContaining({
        where: expect.objectContaining({
          sourceUrl: 'strapi://api::course.course/doc1?locale=en-US',
        }),
      })
    );
    expect(mockDeleteVectors).toHaveBeenCalledWith(5);
    expect(mockDelete).toHaveBeenCalled();
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`cd backend && npx vitest run src/services/__tests__/knowledge-sync-service.test.ts`
预期：FAIL，syncSingleContent 不写入 locale，sourceUrl 不含 locale

- [ ] **步骤 3：修改 knowledge-sync-service.ts**

修改 `backend/src/services/knowledge-sync-service.ts`：

```typescript
type Locale = 'zh-CN' | 'en-US';

function normalizeLocale(input: unknown): Locale {
  if (typeof input === 'string' && (['zh-CN', 'en-US'] as readonly string[]).includes(input)) {
    return input as Locale;
  }
  return 'zh-CN';
}

function buildSourceUrl(uid: string, record: any): string {
  const locale = normalizeLocale(record?.locale);
  const docId = record?.documentId || record?.id;
  return `strapi://${uid}/${docId}?locale=${locale}`;
}

export async function syncSingleContent(strapi: any, uid: string, record: any): Promise<void> {
  const config = CONTENT_TYPES.find(c => c.uid === uid);
  if (!config) return;

  const locale = normalizeLocale(record?.locale);
  const sourceUrl = buildSourceUrl(uid, record);
  const content = config.serialize(record);

  const existing = await strapi.db.query('api::knowledge-base.knowledge-base').findOne({
    where: { sourceUrl },
  });

  if (existing) {
    await strapi.documents('api::knowledge-base.knowledge-base').update({
      documentId: existing.documentId,
      data: {
        title: record.title || record.name || '文档',
        content,
        locale,  // 新增
        status: 'pending',
      },
    });
  } else {
    await strapi.documents('api::knowledge-base.knowledge-base').create({
      data: {
        title: record.title || record.name || '文档',
        content,
        sourceType: 'content-sync',
        sourceUrl,
        locale,  // 新增
        status: 'pending',
        priority: 'high',
        tags: config.name,
      },
    });
  }
}

export async function deleteSyncedContent(strapi: any, uid: string, record: any): Promise<void> {
  const sourceUrl = buildSourceUrl(uid, record);
  const existing = await strapi.db.query('api::knowledge-base.knowledge-base').findOne({
    where: { sourceUrl },
  });
  if (existing) {
    const kbService = strapi.service('api::knowledge-base.knowledge-base');
    await kbService.deleteVectors(existing.id);
    await strapi.documents('api::knowledge-base.knowledge-base').delete({
      documentId: existing.documentId,
    });
  }
}

// syncWebsiteContent 也需要按 locale 遍历，修改 findMany 调用：
export async function syncWebsiteContent(strapi: any): Promise<{ synced: number; updated: number; errors: string[] }> {
  let synced = 0;
  let updated = 0;
  const errors: string[] = [];
  const LOCALES: Locale[] = ['zh-CN', 'en-US'];

  for (const { uid, serialize, name } of CONTENT_TYPES) {
    for (const locale of LOCALES) {
      try {
        const records = await strapi.documents(uid).findMany({
          limit: 1000,
          locale,  // Strapi i18n: only records in this locale
        });
        for (const record of records) {
          const recordWithLocale = { ...record, locale };
          const sourceUrl = buildSourceUrl(uid, recordWithLocale);
          const content = serialize(record);

          const existing = await strapi.db.query('api::knowledge-base.knowledge-base').findOne({
            where: { sourceUrl },
          });

          if (existing) {
            await strapi.documents('api::knowledge-base.knowledge-base').update({
              documentId: existing.documentId,
              data: { title: record.title || record.name || `${name}文档`, content, locale, status: 'pending' },
            });
            updated++;
          } else {
            await strapi.documents('api::knowledge-base.knowledge-base').create({
              data: {
                title: record.title || record.name || `${name}文档`,
                content,
                sourceType: 'content-sync',
                sourceUrl,
                locale,
                status: 'pending',
                priority: 'high',
                tags: name,
              },
            });
            synced++;
          }
        }
      } catch (err) {
        errors.push(`${name}[${locale}]: ${err instanceof Error ? err.message : err}`);
      }
    }
  }

  console.log(`[knowledge-sync-service] Sync complete: ${synced} new, ${updated} updated, ${errors.length} errors`);
  return { synced, updated, errors };
}
```

- [ ] **步骤 4：运行测试验证通过**

运行：`cd backend && npx vitest run src/services/__tests__/knowledge-sync-service.test.ts`
预期：PASS（所有用例包括新增 4 个）

- [ ] **步骤 5：Commit**

```bash
cd backend
git add src/services/knowledge-sync-service.ts src/services/__tests__/knowledge-sync-service.test.ts
git commit -m "feat(backend): knowledge-sync-service 支持 locale

sourceUrl 增加 ?locale=xxx 后缀实现同 documentId 双 locale 独立记录；
syncWebsiteContent 按 zh-CN + en-US 双循环遍历；
deleteSyncedContent 按 documentId + locale 精确删除。"
```

---

## 任务 6：翻译辅助 API

**文件：**
- 创建：`backend/src/api/translation/services/translation.ts`
- 创建：`backend/src/api/translation/controllers/translation.ts`
- 创建：`backend/src/api/translation/routes/translation.ts`
- 创建：`backend/src/api/translation/controllers/__tests__/translation.test.ts`

- [ ] **步骤 1：编写失败的翻译 API 测试**

创建 `backend/src/api/translation/controllers/__tests__/translation.test.ts`：

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';

// Mock controller import will be available after implementation
let translationController: any;

beforeEach(async () => {
  vi.resetModules();
  translationController = (await import('../translation')).default;
});

describe('POST /api/translation/assist', () => {
  function buildCtx(body: any, isAdmin = true): any {
    return {
      request: { body },
      body: null as any,
      throw: vi.fn((status: number, msg: string) => {
        throw new Error(`${status}: ${msg}`);
      }),
      state: { user: isAdmin ? { roles: [{ name: 'strapi-super-admin' }] } : null },
    };
  }

  it('returns 400 when contentType is not in whitelist', async () => {
    const ctx = buildCtx({
      sourceLocale: 'zh-CN',
      targetLocale: 'en-US',
      contentType: 'malicious-type',
      documentId: 'doc1',
      fields: ['name'],
    });
    await translationController.assist(ctx);
    expect(ctx.throw).toHaveBeenCalledWith(400, expect.stringContaining('INVALID_PARAMS'));
  });

  it('returns 400 when fields is empty', async () => {
    const ctx = buildCtx({
      sourceLocale: 'zh-CN',
      targetLocale: 'en-US',
      contentType: 'products',
      documentId: 'doc1',
      fields: [],
    });
    await translationController.assist(ctx);
    expect(ctx.throw).toHaveBeenCalledWith(400, expect.stringContaining('INVALID_PARAMS'));
  });

  it('returns 400 when documentId missing', async () => {
    const ctx = buildCtx({
      sourceLocale: 'zh-CN',
      targetLocale: 'en-US',
      contentType: 'products',
      fields: ['name'],
    });
    await translationController.assist(ctx);
    expect(ctx.throw).toHaveBeenCalledWith(400, expect.stringContaining('INVALID_PARAMS'));
  });

  it('returns 403 when non-admin user', async () => {
    const ctx = buildCtx(
      {
        sourceLocale: 'zh-CN',
        targetLocale: 'en-US',
        contentType: 'products',
        documentId: 'doc1',
        fields: ['name'],
      },
      false
    );
    await translationController.assist(ctx);
    expect(ctx.throw).toHaveBeenCalledWith(403, expect.stringContaining('FORBIDDEN'));
  });

  it('returns 404 when documentId not found in zh-CN', async () => {
    vi.doMock('../../services/translation', () => ({
      translateDocument: vi.fn().mockResolvedValue(null),
    }));
    vi.stubGlobal('strapi', {
      documents: vi.fn().mockReturnValue({
        findOne: vi.fn().mockResolvedValue(null),
      }),
    });

    const ctx = buildCtx({
      sourceLocale: 'zh-CN',
      targetLocale: 'en-US',
      contentType: 'products',
      documentId: 'nonexistent',
      fields: ['name'],
    });
    await translationController.assist(ctx);
    expect(ctx.throw).toHaveBeenCalledWith(404, expect.stringContaining('SOURCE_NOT_FOUND'));
  });

  it('returns translation draft on success', async () => {
    vi.doMock('../../services/translation', () => ({
      translateDocument: vi.fn().mockResolvedValue({
        name: 'English Name',
        description: 'English description',
      }),
    }));
    vi.stubGlobal('strapi', {
      documents: vi.fn().mockReturnValue({
        findOne: vi.fn().mockResolvedValue({ documentId: 'doc1', name: '中文名' }),
      }),
    });

    const ctx = buildCtx({
      sourceLocale: 'zh-CN',
      targetLocale: 'en-US',
      contentType: 'products',
      documentId: 'doc1',
      fields: ['name', 'description'],
    });
    await translationController.assist(ctx);
    expect(ctx.body).toEqual({
      translations: { name: 'English Name', description: 'English description' },
    });
  });

  it('returns 502 when DashScope fails', async () => {
    vi.doMock('../../services/translation', () => ({
      translateDocument: vi.fn().mockRejectedValue(new Error('DashScope timeout')),
    }));
    vi.stubGlobal('strapi', {
      documents: vi.fn().mockReturnValue({
        findOne: vi.fn().mockResolvedValue({ documentId: 'doc1', name: '中文名' }),
      }),
    });

    const ctx = buildCtx({
      sourceLocale: 'zh-CN',
      targetLocale: 'en-US',
      contentType: 'products',
      documentId: 'doc1',
      fields: ['name'],
    });
    await translationController.assist(ctx);
    expect(ctx.throw).toHaveBeenCalledWith(502, expect.stringContaining('AI_PROVIDER_ERROR'));
  });

  it('returns 502 when AI returns JSON missing fields', async () => {
    vi.doMock('../../services/translation', () => ({
      translateDocument: vi.fn().mockResolvedValue({ name: 'English' }), // missing description
    }));
    vi.stubGlobal('strapi', {
      documents: vi.fn().mockReturnValue({
        findOne: vi.fn().mockResolvedValue({ documentId: 'doc1', name: '中文名' }),
      }),
    });

    const ctx = buildCtx({
      sourceLocale: 'zh-CN',
      targetLocale: 'en-US',
      contentType: 'products',
      documentId: 'doc1',
      fields: ['name', 'description'],
    });
    await translationController.assist(ctx);
    expect(ctx.throw).toHaveBeenCalledWith(502, expect.stringContaining('AI_RESPONSE_INCOMPLETE'));
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`cd backend && npx vitest run src/api/translation/controllers/__tests__/translation.test.ts`
预期：FAIL，模块不存在

- [ ] **步骤 3：创建翻译 service**

创建 `backend/src/api/translation/services/translation.ts`：

```typescript
/**
 * Translation assist service.
 *
 * Calls DashScope (qwen-plus) to translate JSON field values from
 * sourceLocale to targetLocale. Returns the translated JSON or throws
 * on provider/parse errors.
 */

const DASHSCOPE_ENDPOINT = 'https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions';
const TRANSLATE_MODEL = 'qwen-plus';
const TIMEOUT_MS = 30000;

const SYSTEM_PROMPT = `You are a professional translator. Translate the JSON values to English. Keep keys unchanged. Return JSON only.`;

export interface TranslateOptions {
  apiKey: string;
  fields: Record<string, string>;
}

export async function translateDocument(opts: TranslateOptions): Promise<Record<string, string>> {
  const controller = new AbortController();
  const timeout = setTimeout(() => controller.abort(), TIMEOUT_MS);

  try {
    const response = await fetch(DASHSCOPE_ENDPOINT, {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${opts.apiKey}`,
      },
      body: JSON.stringify({
        model: TRANSLATE_MODEL,
        messages: [
          { role: 'system', content: SYSTEM_PROMPT },
          { role: 'user', content: JSON.stringify(opts.fields) },
        ],
        temperature: 0.3,
      }),
      signal: controller.signal,
    });

    if (!response.ok) {
      const text = await response.text().catch(() => '');
      throw new Error(`DashScope ${response.status}: ${text}`);
    }

    const data = await response.json();
    const content = data?.choices?.[0]?.message?.content;
    if (!content) {
      throw new Error('AI_RESPONSE_PARSE_ERROR: empty content');
    }

    // Strip markdown code fences if present
    const cleaned = content.replace(/^```(?:json)?\s*/, '').replace(/\s*```$/, '');
    const parsed = JSON.parse(cleaned);
    return parsed;
  } finally {
    clearTimeout(timeout);
  }
}
```

- [ ] **步骤 4：创建翻译 controller**

创建 `backend/src/api/translation/controllers/translation.ts`：

```typescript
/**
 * POST /api/translation/assist
 *
 * Admin-only endpoint that uses DashScope to translate document fields
 * from sourceLocale to targetLocale. Returns a draft JSON — the admin
 * reviews and manually saves it to the en-US locale entry in Strapi.
 */

const CONTENT_TYPE_UIDS: Record<string, string> = {
  products: 'api::product.product',
  news: 'api::news-article.news-article',
  campuses: 'api::campus.campus',
  teachers: 'api::teacher.teacher',
  pages: 'api::page.page',
  faq: 'api::faq-item.faq-item',
};

function isAdmin(ctx: any): boolean {
  const user = ctx.state?.user;
  if (!user) return false;
  const roles = user.roles || [];
  return roles.some((r: any) => r.name === 'strapi-super-admin' || r.name === 'strapi-editor');
}

export default {
  async assist(ctx: any) {
    // 1. Permission check
    if (!isAdmin(ctx)) {
      ctx.throw(403, JSON.stringify({ error: { code: 'FORBIDDEN' } }));
      return;
    }

    // 2. Input validation
    const { sourceLocale, targetLocale, contentType, documentId, fields } = ctx.request.body || {};

    if (!contentType || !(contentType in CONTENT_TYPE_UIDS)) {
      ctx.throw(400, JSON.stringify({ error: { code: 'INVALID_PARAMS', message: 'contentType not in whitelist' } }));
      return;
    }
    if (!documentId) {
      ctx.throw(400, JSON.stringify({ error: { code: 'INVALID_PARAMS', message: 'documentId required' } }));
      return;
    }
    if (!Array.isArray(fields) || fields.length === 0) {
      ctx.throw(400, JSON.stringify({ error: { code: 'INVALID_PARAMS', message: 'fields must be non-empty array' } }));
      return;
    }

    // 3. Read source document
    const uid = CONTENT_TYPE_UIDS[contentType];
    const sourceDoc = await strapi.documents(uid).findOne({
      documentId,
      locale: sourceLocale || 'zh-CN',
    });

    if (!sourceDoc) {
      ctx.throw(404, JSON.stringify({ error: { code: 'SOURCE_NOT_FOUND' } }));
      return;
    }

    // 4. Extract fields to translate
    const fieldsToTranslate: Record<string, string> = {};
    for (const field of fields) {
      const value = (sourceDoc as any)[field];
      if (typeof value === 'string' && value.length > 0) {
        fieldsToTranslate[field] = value;
      }
    }

    if (Object.keys(fieldsToTranslate).length === 0) {
      ctx.body = { translations: {} };
      return;
    }

    // 5. Call DashScope via translation service
    const apiKey = process.env.DASHSCOPE_API_KEY;
    if (!apiKey) {
      ctx.throw(502, JSON.stringify({ error: { code: 'AI_PROVIDER_ERROR', message: 'DASHSCOPE_API_KEY not set' } }));
      return;
    }

    const { translateDocument } = require('../../services/translation') as {
      translateDocument: (opts: { apiKey: string; fields: Record<string, string> }) => Promise<Record<string, string>>;
    };

    let translations: Record<string, string>;
    try {
      translations = await translateDocument({ apiKey, fields: fieldsToTranslate });
    } catch (err) {
      const message = err instanceof Error ? err.message : String(err);
      if (message.includes('AI_RESPONSE_PARSE_ERROR')) {
        ctx.throw(502, JSON.stringify({ error: { code: 'AI_RESPONSE_PARSE_ERROR' } }));
      } else {
        ctx.throw(502, JSON.stringify({ error: { code: 'AI_PROVIDER_ERROR', message: '上游服务异常' } }));
      }
      return;
    }

    // 6. Field completeness check
    const missingFields = fields.filter(f => !(f in translations));
    if (missingFields.length > 0) {
      console.error(`[translation] AI response missing fields: ${missingFields.join(', ')}`);
      ctx.throw(502, JSON.stringify({ error: { code: 'AI_RESPONSE_INCOMPLETE', message: `Missing: ${missingFields.join(', ')}` } }));
      return;
    }

    ctx.body = { translations };
  },
};
```

- [ ] **步骤 5：创建路由**

创建 `backend/src/api/translation/routes/translation.ts`：

```typescript
export default {
  routes: [
    {
      method: 'POST',
      path: '/translation/assist',
      handler: 'translation.assist',
      config: {
        auth: true,  // 需要认证
        policies: [],
      },
    },
  ],
};
```

- [ ] **步骤 6：运行测试验证通过**

运行：`cd backend && npx vitest run src/api/translation/controllers/__tests__/translation.test.ts`
预期：PASS（8 个用例）

- [ ] **步骤 7：Commit**

```bash
cd backend
git add src/api/translation/
git commit -m "feat(backend): 翻译辅助 API /api/translation/assist

admin-only，复用 DashScope qwen-plus 翻译 JSON 字段；
字段完整性校验（缺失返回 502 AI_RESPONSE_INCOMPLETE）；
不自动重试（避免双倍计费），管理员手动重试。"
```

---

## 任务 7：前端 next-intl 基础设施

**文件：**
- 创建：`frontend-next/i18n/routing.ts`
- 创建：`frontend-next/i18n/request.ts`
- 创建：`frontend-next/i18n/messages/zh-CN.json`
- 创建：`frontend-next/i18n/messages/en-US.json`
- 创建：`frontend-next/i18n/messages/__tests__/messages.test.ts`
- 创建：`frontend-next/middleware.ts`
- 修改：`frontend-next/next.config.ts`

- [ ] **步骤 1：编写失败的 messages 完整性测试**

创建 `frontend-next/i18n/messages/__tests__/messages.test.ts`：

```typescript
import zhCN from '../zh-CN.json';
import enUS from '../en-US.json';

function flattenKeys(obj: Record<string, unknown>, prefix = ''): string[] {
  const keys: string[] = [];
  for (const [key, value] of Object.entries(obj)) {
    const fullKey = prefix ? `${prefix}.${key}` : key;
    if (typeof value === 'object' && value !== null && !Array.isArray(value)) {
      keys.push(...flattenKeys(value as Record<string, unknown>, fullKey));
    } else {
      keys.push(fullKey);
    }
  }
  return keys;
}

describe('i18n messages key completeness', () => {
  const zhKeys = flattenKeys(zhCN as Record<string, unknown>);
  const enKeys = flattenKeys(enUS as Record<string, unknown>);

  it('zh-CN has all keys that en-US has', () => {
    const missing = enKeys.filter(k => !zhKeys.includes(k));
    expect(missing).toEqual([]);
  });

  it('en-US has all keys that zh-CN has', () => {
    const missing = zhKeys.filter(k => !enKeys.includes(k));
    expect(missing).toEqual([]);
  });

  it('both have at least the core namespaces', () => {
    expect(zhCN).toHaveProperty('common');
    expect(zhCN).toHaveProperty('navigation');
    expect(zhCN).toHaveProperty('footer');
    expect(zhCN).toHaveProperty('chat');
    expect(enUS).toHaveProperty('common');
    expect(enUS).toHaveProperty('navigation');
    expect(enUS).toHaveProperty('footer');
    expect(enUS).toHaveProperty('chat');
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`cd frontend-next && npx jest i18n/messages/__tests__/messages.test.ts`
预期：FAIL，找不到 zh-CN.json / en-US.json

- [ ] **步骤 3：创建 messages JSON 文件**

创建 `frontend-next/i18n/messages/zh-CN.json`：

```json
{
  "common": {
    "i18nFallbackNotice": "此内容暂无英文版，已为你显示中文版本",
    "emptyList": "暂无内容",
    "loading": "加载中..."
  },
  "navigation": {
    "home": "首页",
    "courses": "课程",
    "news": "新闻动态",
    "campuses": "校区环境",
    "teachers": "师资团队",
    "about": "关于我们",
    "contact": "联系我们",
    "faq": "常见问题",
    "appointment": "预约试听",
    "onlineConsult": "在线咨询"
  },
  "footer": {
    "copyright": "版权所有",
    "quickLinks": "快速链接",
    "contactUs": "联系我们"
  },
  "chat": {
    "welcome": "您好！我是佑森小课堂的AI助手，有什么可以帮您的吗？",
    "transferPrompt": "好的，正在为您转接人工客服，请稍候...",
    "thresholdPrompt": "您今天已经咨询了很多问题，为了更好地为您服务，建议您预约一次免费试听课，我们的顾问会为您详细解答。",
    "guidancePrompt": "这个问题我需要转给人工客服为您解答。您也可以先留下联系方式，我们的顾问会尽快联系您。",
    "fallbackAnswer": "Sorry, I cannot answer this question. Would you like to transfer to a human agent?",
    "inputPlaceholder": "请输入您的问题...",
    "sendButton": "发送"
  },
  "languageSwitcher": {
    "label": "切换语言",
    "chinese": "中文",
    "english": "English"
  },
  "courses": {
    "title": "课程体系",
    "viewDetails": "查看详情"
  },
  "news": {
    "title": "新闻动态"
  },
  "appointment": {
    "title": "预约试听课",
    "parentName": "家长姓名",
    "childName": "孩子姓名",
    "phone": "手机号码",
    "campus": "选择校区",
    "submit": "提交预约"
  }
}
```

创建 `frontend-next/i18n/messages/en-US.json`：

```json
{
  "common": {
    "i18nFallbackNotice": "This content is not available in English yet. Showing the Chinese version.",
    "emptyList": "No content available",
    "loading": "Loading..."
  },
  "navigation": {
    "home": "Home",
    "courses": "Courses",
    "news": "News",
    "campuses": "Campuses",
    "teachers": "Teachers",
    "about": "About Us",
    "contact": "Contact",
    "faq": "FAQ",
    "appointment": "Book a Trial",
    "onlineConsult": "Online Consult"
  },
  "footer": {
    "copyright": "All rights reserved",
    "quickLinks": "Quick Links",
    "contactUs": "Contact Us"
  },
  "chat": {
    "welcome": "Hello! I'm the Yousen AI assistant. How can I help you?",
    "transferPrompt": "Sure, transferring you to a human agent. Please wait...",
    "thresholdPrompt": "You've asked many questions today. To serve you better, we suggest booking a free trial class. Our consultant will assist you in detail.",
    "guidancePrompt": "I need to transfer this question to a human agent. You can also leave your contact information and our consultant will reach out soon.",
    "fallbackAnswer": "Sorry, I cannot answer this question. Would you like to transfer to a human agent?",
    "inputPlaceholder": "Type your question...",
    "sendButton": "Send"
  },
  "languageSwitcher": {
    "label": "Switch Language",
    "chinese": "中文",
    "english": "English"
  },
  "courses": {
    "title": "Our Courses",
    "viewDetails": "View Details"
  },
  "news": {
    "title": "News & Updates"
  },
  "appointment": {
    "title": "Book a Trial Class",
    "parentName": "Parent Name",
    "childName": "Child Name",
    "phone": "Phone Number",
    "campus": "Select Campus",
    "submit": "Submit Booking"
  }
}
```

- [ ] **步骤 4：运行测试验证通过**

运行：`cd frontend-next && npx jest i18n/messages/__tests__/messages.test.ts`
预期：PASS（3 个用例）

- [ ] **步骤 5：创建 routing.ts**

创建 `frontend-next/i18n/routing.ts`：

```typescript
import { defineRouting } from 'next-intl/routing';

export const routing = defineRouting({
  locales: ['zh-CN', 'en-US'],
  defaultLocale: 'zh-CN',
  localePrefix: 'as-needed',
});

export type Locale = (typeof routing.locales)[number];
```

- [ ] **步骤 6：创建 request.ts**

创建 `frontend-next/i18n/request.ts`：

```typescript
import { getRequestConfig } from 'next-intl/server';
import { routing } from './routing';

export default getRequestConfig(async ({ requestLocale }) => {
  let locale = await requestLocale;

  // Ensure locale is one of our supported locales
  if (!locale || !routing.locales.includes(locale as any)) {
    locale = routing.defaultLocale;
  }

  return {
    locale,
    messages: (await import(`./messages/${locale}.json`)).default,
    onError(error: Error) {
      if (process.env.NODE_ENV === 'production') {
        console.error('[i18n]', error.message);
      } else {
        throw error;
      }
    },
    getMessageFallback({ key }: { key: string }) {
      return key;
    },
  };
});
```

- [ ] **步骤 7：创建 middleware.ts**

创建 `frontend-next/middleware.ts`：

```typescript
import createMiddleware from 'next-intl/middleware';
import { routing } from './i18n/routing';

export default createMiddleware(routing);

export const config = {
  // Match all pathnames except for
  // - API routes
  // - Next.js internals (_next, _vercel)
  // - Static files (with file extensions)
  // - llms.txt, robots.txt, sitemap.xml
  matcher: ['/((?!api|_next|_vercel|.*\\..*|llms\\.txt|robots\\.txt|sitemap\\.xml).*)'],
};
```

- [ ] **步骤 8：修改 next.config.ts**

修改 `frontend-next/next.config.ts`，在 plugins 数组添加 next-intl：

```typescript
// 在文件顶部添加 import：
import createNextIntlPlugin from 'next-intl/plugin';

const withNextIntl = createNextIntlPlugin('./i18n/request.ts');

// 在文件末尾，将 export default nextConfig 改为：
export default withNextIntl(nextConfig);
```

- [ ] **步骤 9：安装 next-intl 依赖**

运行：`cd frontend-next && npm install next-intl`

- [ ] **步骤 10：Commit**

```bash
cd frontend-next
git add i18n/ middleware.ts next.config.ts package.json package-lock.json
git commit -m "feat(frontend): next-intl 基础设施

routing.ts: localePrefix 'as-needed'（中文无前缀、英文 /en-US/）；
request.ts: getRequestConfig + onError 生产环境降级；
middleware.ts: 排除 /api、/_next、/llms.txt 等；
messages JSON: 中英文 UI 文案 + 双向 key 完整性测试。"
```

---

## 任务 8：前端目录迁移 app/ → app/[locale]/

**文件：**
- 修改：`frontend-next/app/layout.tsx`（改为壳层）
- 创建：`frontend-next/app/[locale]/layout.tsx`（由原 layout.tsx 迁入）
- 迁移：所有 `app/*.tsx` → `app/[locale]/*.tsx`

**注意：** 此任务文件操作量大但机械性强，无新增测试（保持现有测试通过即验证）。

- [ ] **步骤 1：将原 app/layout.tsx 改为最小壳层**

修改 `frontend-next/app/layout.tsx`：

```typescript
import './globals.css';

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html>
      <body>{children}</body>
    </html>
  );
}
```

- [ ] **步骤 2：创建 app/[locale]/layout.tsx**

创建 `frontend-next/app/[locale]/layout.tsx`，将原 `app/layout.tsx` 的完整逻辑迁入，并增加：

```typescript
import type { Metadata } from 'next';
import { Noto_Sans_SC, Nunito } from 'next/font/google';
import { setRequestLocale, getTranslations } from 'next-intl/server';
import { NextIntlClientProvider } from 'next-intl';
import { getSiteSettings, getNavigationTree, getFooter, getImageUrl } from '@/lib/api';
import LayoutShell from '@/components/layout/LayoutShell';
import Navigation from '@/components/layout/Navigation';
import Footer from '@/components/layout/Footer';
import FloatingChat from '@/components/chat/FloatingChat';
import LanguageSwitcher from '@/components/layout/LanguageSwitcher';
import '../globals.css';

export const revalidate = 300;

const notoSansSC = Noto_Sans_SC({
  weight: ['400', '500', '700'],
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-default',
  preload: true,
});

const nunito = Nunito({
  weight: ['400', '600', '700', '800'],
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-nunito',
  preload: true,
});

export async function generateMetadata({ params }: { params: Promise<{ locale: string }> }): Promise<Metadata> {
  const { locale } = await params;
  return {
    title: {
      default: locale === 'en-US' ? 'Yousen Education | Preschool-Primary Transition' : '佑森小课堂 | 专注幼小衔接教育8年',
      template: locale === 'en-US' ? '%s | Yousen Education' : '%s | 佑森小课堂',
    },
    openGraph: {
      type: 'website',
      locale: locale === 'en-US' ? 'en_US' : 'zh_CN',
      siteName: locale === 'en-US' ? 'Yousen Education' : '佑森小课堂',
    },
    robots: { index: true, follow: true },
  };
}

export default async function LocaleLayout({
  children,
  params,
}: {
  children: React.ReactNode;
  params: Promise<{ locale: string }>;
}) {
  const { locale } = await params;
  setRequestLocale(locale);

  const [settingsRes, navRes, footerRes, messages] = await Promise.all([
    getSiteSettings(locale as any).catch(() => ({ data: [] as never[] })),
    getNavigationTree(locale as any).catch(() => ({ data: [] as never[] })),
    getFooter(locale as any).catch(() => ({ data: [] as never[] })),
    getTranslations(),
  ]);

  const settings = Array.isArray(settingsRes.data) ? settingsRes.data[0] : settingsRes.data;
  const navigation = navRes.data;
  const footer = Array.isArray(footerRes.data) ? footerRes.data[0] : footerRes.data;

  // ... 原有 fontSettings 逻辑保持不变 ...

  return (
    <html
      lang={locale}
      className={`${notoSansSC.variable} ${nunito.variable}`}
      style={{ fontFamily }}
    >
      <head>
        {/* 原有 fontFaceCSS 逻辑 */}
      </head>
      <body>
        <NextIntlClientProvider locale={locale} messages={messages}>
          <LayoutShell>
            <Navigation navigation={navigation} settings={settings} locale={locale} />
            <main className="flex-1">{children}</main>
            <Footer footer={footer} settings={settings} />
          </LayoutShell>
          <FloatingChat locale={locale as any} />
        </NextIntlClientProvider>
      </body>
    </html>
  );
}
```

- [ ] **步骤 3：迁移所有页面文件**

将以下文件/目录从 `app/` 移到 `app/[locale]/`：
- `page.tsx`
- `courses/`
- `news/`
- `campuses/`
- `teachers/`
- `appointment/`
- `appointment-success/`
- `contact/`
- `faq/`
- `privacy-policy/`
- `refund-policy/`
- `user-agreement/`
- `[slug]/page.tsx`
- `error.tsx`
- `global-error.tsx`
- `loading.tsx`
- `not-found.tsx`

运行命令（在 frontend-next 目录下）：

```bash
cd frontend-next/app
mkdir -p [locale]
mv page.tsx courses news campuses teachers appointment appointment-success contact faq privacy-policy refund-policy user-agreement [locale]/
mv error.tsx global-error.tsx loading.tsx not-found.tsx [locale]/
# [slug] 目录需要特殊处理（glob 模式）
mv [slug] [locale]/
```

- [ ] **步骤 4：在每个 [locale]/page.tsx 顶部添加 setRequestLocale**

对每个动态路由页面（`[slug]/page.tsx`、`courses/[slug]/page.tsx` 等），在页面函数开头添加：

```typescript
import { setRequestLocale } from 'next-intl/server';

export default async function Page({ params }: { params: Promise<{ locale: string; slug: string }> }) {
  const { locale, slug } = await params;
  setRequestLocale(locale);
  // ... 原有逻辑
}
```

对每个 `generateStaticParams` 函数，扩展为返回 locale + slug：

```typescript
export async function generateStaticParams() {
  const products = await getProducts();
  return products.data.map(p => [
    { locale: 'zh-CN', slug: p.slug },
    { locale: 'en-US', slug: p.slug },
  ]).flat();
}
```

- [ ] **步骤 5：运行现有测试验证无回归**

运行：`cd frontend-next && npx jest`
预期：现有测试全部通过（可能需要调整 mock 路径）

- [ ] **步骤 6：Commit**

```bash
cd frontend-next
git add app/
git commit -m "refactor(frontend): app/ 目录迁移到 app/[locale]/

引入 [locale] 动态段，根 layout 改为最小壳层；
所有页面迁入 [locale]/，添加 setRequestLocale + generateStaticParams 扩展；
NextIntlClientProvider 包裹 LayoutShell + FloatingChat。"
```

---

## 任务 9：lib/api.ts locale 参数 + fallback

**文件：**
- 修改：`frontend-next/lib/api.ts`
- 修改：`frontend-next/lib/__tests__/api.test.ts`

- [ ] **步骤 1：编写失败的 api locale 测试**

在 `frontend-next/lib/__tests__/api.test.ts` 追加：

```typescript
describe('lib/api locale support', () => {
  beforeEach(() => {
    vi.resetModules();
    vi.mock('@sentry/nextjs', () => ({ captureException: vi.fn() }));
  });

  it('getProducts appends locale=en-US to URL', async () => {
    const mockFetch = vi.fn().mockResolvedValue({
      ok: true,
      json: () => Promise.resolve({ data: [] }),
      headers: { get: () => null },
    });
    vi.stubGlobal('fetch', mockFetch);

    const { getProducts } = await import('../api');
    await getProducts('en-US');
    const url = mockFetch.mock.calls[0][0] as string;
    expect(url).toContain('locale=en-US');
  });

  it('getProductBySlug falls back to zh-CN when en-US returns 404', async () => {
    const mockFetch = vi.fn()
      .mockResolvedValueOnce({
        ok: false,
        status: 404,
        text: () => Promise.resolve('not found'),
        headers: { get: () => null },
      })
      .mockResolvedValueOnce({
        ok: true,
        json: () => Promise.resolve({ data: { id: 1, name: '中文课程' } }),
        headers: { get: () => null },
      });
    vi.stubGlobal('fetch', mockFetch);

    const { getProductBySlug } = await import('../api');
    const result = await getProductBySlug('test-slug', 'en-US');
    expect(result.data.name).toBe('中文课程');
    expect(result._i18nFallback).toBe(true);
    expect(mockFetch).toHaveBeenCalledTimes(2);
  });

  it('getProducts does NOT fallback when en-US returns empty array', async () => {
    const mockFetch = vi.fn().mockResolvedValue({
      ok: true,
      json: () => Promise.resolve({ data: [] }),
      headers: { get: () => null },
    });
    vi.stubGlobal('fetch', mockFetch);

    const { getProducts } = await import('../api');
    const result = await getProducts('en-US');
    expect(result.data).toEqual([]);
    expect(mockFetch).toHaveBeenCalledTimes(1);
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`cd frontend-next && npx jest lib/__tests__/api.test.ts`
预期：FAIL，getProducts 不接受 locale 参数

- [ ] **步骤 3：修改 lib/api.ts 所有函数**

在 `frontend-next/lib/api.ts` 顶部添加类型：

```typescript
export type Locale = 'zh-CN' | 'en-US';
```

修改每个函数，以 `getProducts` 和 `getProductBySlug` 为例：

```typescript
export async function getProducts(locale: Locale = 'zh-CN', filters?: { category?: string }) {
  const params = new URLSearchParams();
  params.set('populate', 'thumbnail,categories,specs,objectives,outline,testimonials');
  params.set('locale', locale);
  if (filters?.category) {
    params.set('category', filters.category);
  }
  return fetchApi<{ data: Product[]; meta: Pagination }>(`/api/products?${params.toString()}`);
}

export async function getProductBySlug(slug: string, locale: Locale = 'zh-CN') {
  try {
    return await fetchApi<{ data: Product }>(`/api/products/slug/${slug}?locale=${locale}`);
  } catch (err) {
    // Fallback to zh-CN for detail pages when en-US is missing
    if (locale !== 'zh-CN' && err instanceof Error && err.message.includes('404')) {
      console.warn(`[i18n] ${locale} missing, fallback to zh-CN: products/${slug}`);
      const result = await fetchApi<{ data: Product }>(`/api/products/slug/${slug}?locale=zh-CN`);
      return { ...result, _i18nFallback: true } as { data: Product; _i18nFallback: boolean };
    }
    throw err;
  }
}
```

对以下函数同样添加 `locale` 参数（无 fallback，仅传参）：
- `getSiteSettings(locale)`
- `getNavigationTree(locale)`
- `getFooter(locale)`
- `getNews(locale, category?)`
- `getNewsBySlug(slug, locale)` — 带 fallback
- `getCampuses(locale)`
- `getCampusBySlug(slug, locale)` — 带 fallback
- `getTeachers(locale)`
- `getTeacherBySlug(slug, locale)` — 带 fallback
- `getPageBySlug(slug, locale)` — 带 fallback
- `getFaqItems(locale, category?)`
- `getTestimonials(locale)`
- `getHomepage(locale)`

- [ ] **步骤 4：运行测试验证通过**

运行：`cd frontend-next && npx jest lib/__tests__/api.test.ts`
预期：PASS（所有用例包括新增 3 个）

- [ ] **步骤 5：Commit**

```bash
cd frontend-next
git add lib/api.ts lib/__tests__/api.test.ts
git commit -m "feat(frontend): lib/api.ts 全函数支持 locale 参数

所有 API 函数接受 locale 参数并追加到 URL；
detail 页函数（getProductBySlug 等）en-US 404 时 fallback 到 zh-CN；
返回 _i18nFallback: true 标记，供页面显示 banner；
list 页函数不 fallback。"
```

---

## 任务 10：LanguageSwitcher 组件 + Navigation/Footer 改造

**文件：**
- 创建：`frontend-next/components/layout/LanguageSwitcher.tsx`
- 创建：`frontend-next/components/layout/__tests__/LanguageSwitcher.test.tsx`
- 修改：`frontend-next/components/layout/Navigation.tsx`
- 修改：`frontend-next/components/chat/FloatingChat.tsx`
- 修改：`frontend-next/lib/chat.ts`

- [ ] **步骤 1：编写失败的 LanguageSwitcher 测试**

创建 `frontend-next/components/layout/__tests__/LanguageSwitcher.test.tsx`：

```typescript
import { render, screen, fireEvent } from '@testing-library/react';
import { useRouter, usePathname } from 'next/navigation';
import { useLocale } from 'next-intl';
import LanguageSwitcher from '../LanguageSwitcher';

vi.mock('next/navigation');
vi.mock('next-intl');

describe('LanguageSwitcher', () => {
  beforeEach(() => {
    vi.clearAllMocks();
  });

  it('renders with aria-label', () => {
    (useLocale as any).mockReturnValue('zh-CN');
    (useRouter as any).mockReturnValue({ replace: vi.fn() });
    (usePathname as any).mockReturnValue('/courses');

    render(<LanguageSwitcher />);
    expect(screen.getByLabelText('切换语言')).toBeInTheDocument();
  });

  it('highlights current locale zh-CN', () => {
    (useLocale as any).mockReturnValue('zh-CN');
    (useRouter as any).mockReturnValue({ replace: vi.fn() });
    (usePathname as any).mockReturnValue('/courses');

    render(<LanguageSwitcher />);
    const zhButton = screen.getByText('中文');
    expect(zhButton).toHaveAttribute('aria-current', 'true');
  });

  it('calls router.replace with en-US when clicking English', () => {
    (useLocale as any).mockReturnValue('zh-CN');
    const mockReplace = vi.fn();
    (useRouter as any).mockReturnValue({ replace: mockReplace });
    (usePathname as any).mockReturnValue('/courses');

    render(<LanguageSwitcher />);
    fireEvent.click(screen.getByText('English'));
    expect(mockReplace).toHaveBeenCalledWith('/courses', { locale: 'en-US' });
  });

  it('writes NEXT_LOCALE cookie on switch', () => {
    (useLocale as any).mockReturnValue('zh-CN');
    (useRouter as any).mockReturnValue({ replace: vi.fn() });
    (usePathname as any).mockReturnValue('/courses');

    render(<LanguageSwitcher />);
    fireEvent.click(screen.getByText('English'));
    expect(document.cookie).toContain('NEXT_LOCALE=en-US');
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`cd frontend-next && npx jest components/layout/__tests__/LanguageSwitcher.test.tsx`
预期：FAIL，找不到 LanguageSwitcher 组件

- [ ] **步骤 3：创建 LanguageSwitcher 组件**

创建 `frontend-next/components/layout/LanguageSwitcher.tsx`：

```typescript
'use client';

import { useState, useRef, useEffect } from 'react';
import { useRouter, usePathname } from 'next/navigation';
import { useLocale } from 'next-intl';
import { Globe, ChevronDown } from 'lucide-react';

export default function LanguageSwitcher() {
  const locale = useLocale() as 'zh-CN' | 'en-US';
  const router = useRouter();
  const pathname = usePathname();
  const [isOpen, setIsOpen] = useState(false);
  const dropdownRef = useRef<HTMLDivElement>(null);

  useEffect(() => {
    function handleClickOutside(e: MouseEvent) {
      if (dropdownRef.current && !dropdownRef.current.contains(e.target as Node)) {
        setIsOpen(false);
      }
    }
    document.addEventListener('mousedown', handleClickOutside);
    return () => document.removeEventListener('mousedown', handleClickOutside);
  }, []);

  function switchTo(targetLocale: 'zh-CN' | 'en-US') {
    if (targetLocale === locale) {
      setIsOpen(false);
      return;
    }
    // Set cookie (1 year)
    document.cookie = `NEXT_LOCALE=${targetLocale}; path=/; max-age=${60 * 60 * 24 * 365}; samesite=lax`;
    // Strip current locale prefix from pathname for next-intl router.replace
    const cleanPath = pathname.replace(/^\/en-US/, '') || '/';
    router.replace(cleanPath, { locale: targetLocale });
    setIsOpen(false);
  }

  return (
    <div ref={dropdownRef} className="relative">
      <button
        type="button"
        aria-label="切换语言"
        onClick={() => setIsOpen(!isOpen)}
        className="flex items-center gap-1 px-3 py-2 text-sm text-muted-foreground hover:text-foreground transition-colors"
      >
        <Globe className="w-4 h-4" />
        <span>{locale === 'zh-CN' ? '中文' : 'English'}</span>
        <ChevronDown className="w-3 h-3" />
      </button>
      {isOpen && (
        <div className="absolute right-0 mt-2 w-32 bg-white border border-border rounded-md shadow-lg z-50">
          <button
            type="button"
            aria-current={locale === 'zh-CN' ? 'true' : 'false'}
            onClick={() => switchTo('zh-CN')}
            className="block w-full text-left px-3 py-2 text-sm hover:bg-muted transition-colors"
          >
            中文
          </button>
          <button
            type="button"
            aria-current={locale === 'en-US' ? 'true' : 'false'}
            onClick={() => switchTo('en-US')}
            className="block w-full text-left px-3 py-2 text-sm hover:bg-muted transition-colors"
          >
            English
          </button>
        </div>
      )}
    </div>
  );
}
```

- [ ] **步骤 4：修改 Navigation.tsx 插入 LanguageSwitcher**

修改 `frontend-next/components/layout/Navigation.tsx`，在桌面端导航栏右侧（在线咨询按钮前）插入：

```typescript
// 在 props 接口添加 locale（可选，因为 LanguageSwitcher 自己用 useLocale）
// 在导航栏右侧区域添加：
import LanguageSwitcher from './LanguageSwitcher';

// 在 JSX 中，"在线咨询" 按钮前：
<LanguageSwitcher />
```

- [ ] **步骤 5：修改 FloatingChat.tsx 接受 locale prop**

修改 `frontend-next/components/chat/FloatingChat.tsx`：

```typescript
// 修改 props 接口
export default function FloatingChat({ locale = 'zh-CN' }: { locale?: 'zh-CN' | 'en-US' }) {
  // ...
  const WELCOME_MESSAGE = locale === 'en-US'
    ? 'Hello! I\'m the Yousen AI assistant. How can I help you?'
    : '您好！我是佑森小课堂的AI助手，有什么可以帮您的吗？';

  const initializeSession = useCallback(async () => {
    // ...
    const result = await startChat({
      sourcePage: typeof window !== 'undefined' ? window.location.pathname : '/',
      locale,  // 新增
    });
    // ...
  }, [locale]);
}
```

- [ ] **步骤 6：修改 lib/chat.ts 接受 locale**

修改 `frontend-next/lib/chat.ts`：

```typescript
export async function startChat(options: {
  sourcePage?: string;
  locale?: 'zh-CN' | 'en-US';
} = {}): Promise<{ sessionId: string; visitorId: string }> {
  const res = await fetch('/api/chat/start', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      sourcePage: options.sourcePage,
      locale: options.locale || 'zh-CN',
    }),
  });
  // ...
}

export async function sendMessage(sessionId: string, message: string, locale?: 'zh-CN' | 'en-US'): Promise<ChatMessageData> {
  // locale 仅用于 UI 提示语选择，session.locale 在后端防篡改
  // ...
}
```

- [ ] **步骤 7：运行测试验证通过**

运行：`cd frontend-next && npx jest components/layout/__tests__/LanguageSwitcher.test.tsx`
预期：PASS（4 个用例）

- [ ] **步骤 8：Commit**

```bash
cd frontend-next
git add components/layout/LanguageSwitcher.tsx components/layout/__tests__/LanguageSwitcher.test.tsx components/layout/Navigation.tsx components/chat/FloatingChat.tsx lib/chat.ts
git commit -m "feat(frontend): LanguageSwitcher 组件 + Navigation/FloatingChat 双语

LanguageSwitcher: 下拉菜单切换 + Cookie 持久化 + aria-label；
Navigation: 右侧插入 LanguageSwitcher；
FloatingChat: 接受 locale prop，欢迎语 + startChat 传 locale。"
```

---

## 任务 11：SEO 改造（hreflang + sitemap + llms.txt）

**文件：**
- 修改：`frontend-next/lib/seo.ts`
- 修改：`frontend-next/lib/__tests__/seo.test.ts`
- 修改：`frontend-next/app/sitemap.ts`
- 修改：`frontend-next/app/llms.txt/route.ts`

- [ ] **步骤 1：编写失败的 seo hreflang 测试**

在 `frontend-next/lib/__tests__/seo.test.ts` 追加：

```typescript
describe('buildMetadata hreflang injection', () => {
  it('injects alternates.languages with zh-CN + en-US URLs', () => {
    const { buildMetadata } = require('../seo');
    const metadata = buildMetadata(undefined, {
      title: 'Courses',
      canonicalUrl: 'https://example.com/courses',
    }, { locale: 'zh-CN', path: '/courses' });

    expect(metadata.alternates?.languages).toEqual({
      'zh-CN': 'https://example.com/courses',
      'en-US': 'https://example.com/en-US/courses',
    });
  });

  it('injects alternates.languages for en-US locale', () => {
    const { buildMetadata } = require('../seo');
    const metadata = buildMetadata(undefined, {
      title: 'Courses',
      canonicalUrl: 'https://example.com/en-US/courses',
    }, { locale: 'en-US', path: '/courses' });

    expect(metadata.alternates?.languages).toEqual({
      'zh-CN': 'https://example.com/courses',
      'en-US': 'https://example.com/en-US/courses',
    });
  });

  it('does not inject alternates.languages when path missing', () => {
    const { buildMetadata } = require('../seo');
    const metadata = buildMetadata(undefined, { title: 'Home' });
    expect(metadata.alternates?.languages).toBeUndefined();
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`cd frontend-next && npx jest lib/__tests__/seo.test.ts`
预期：FAIL，buildMetadata 不接受第三参数

- [ ] **步骤 3：修改 buildMetadata 接受 locale + path**

修改 `frontend-next/lib/seo.ts`：

```typescript
export function buildMetadata(
  seo: SeoData | undefined,
  fallback: { title: string; description?: string; canonicalUrl?: string },
  i18n?: { locale: 'zh-CN' | 'en-US'; path: string }
): Metadata {
  const title = seo?.metaTitle ?? fallback.title;
  const description = seo?.metaDescription ?? fallback.description;
  const ogImage = getImageUrl(seo?.ogImage);

  const baseUrl = process.env.NEXT_PUBLIC_SITE_URL || 'http://localhost:3000';
  const languages = i18n
    ? {
        'zh-CN': `${baseUrl}${i18n.path}`,
        'en-US': `${baseUrl}/en-US${i18n.path}`,
      }
    : undefined;

  return {
    title,
    description,
    keywords: seo?.metaKeywords,
    alternates: {
      canonical: seo?.canonicalUrl || fallback.canonicalUrl,
      languages,
    },
    openGraph: {
      title: seo?.ogTitle ?? title,
      description: seo?.ogDescription ?? description,
      images: ogImage ? [{ url: ogImage }] : undefined,
      type: resolveOgType(seo?.ogType),
    },
    twitter: {
      card: 'summary_large_image',
      images: ogImage ? [ogImage] : undefined,
    },
  };
}
```

- [ ] **步骤 4：修改 sitemap.ts 输出 hreflang**

修改 `frontend-next/app/sitemap.ts`：

```typescript
import type { MetadataRoute } from 'next';
import { getProducts, getNews } from '@/lib/api';

export const revalidate = 3600;

export default async function sitemap(): Promise<MetadataRoute.Sitemap> {
  const baseUrl = process.env.NEXT_PUBLIC_SITE_URL || 'http://localhost:3000';
  const entries: MetadataRoute.Sitemap = [];

  // Static pages — output both zh-CN and en-US URLs with hreflang
  const staticPaths = ['', '/courses', '/news', '/campuses', '/teachers', '/faq', '/refund-policy', '/privacy-policy', '/user-agreement'];
  staticPaths.forEach((path) => {
    const zhUrl = `${baseUrl}${path}`;
    const enUrl = `${baseUrl}/en-US${path}`;
    entries.push({
      url: zhUrl,
      lastModified: new Date(),
      priority: path === '' ? 1.0 : 0.8,
      changeFrequency: 'daily',
      alternates: {
        languages: {
          'zh-CN': zhUrl,
          'en-US': enUrl,
        },
      },
    });
    entries.push({
      url: enUrl,
      lastModified: new Date(),
      priority: path === '' ? 0.9 : 0.7,
      alternates: {
        languages: {
          'zh-CN': zhUrl,
          'en-US': enUrl,
        },
      },
    });
  });

  // Dynamic pages (products, news) — same pattern
  const { data: products } = await getProducts().catch(() => ({ data: [] as never[] }));
  products.forEach((p) => {
    const zhUrl = `${baseUrl}/courses/${p.slug}`;
    const enUrl = `${baseUrl}/en-US/courses/${p.slug}`;
    [zhUrl, enUrl].forEach((url) => {
      entries.push({
        url,
        lastModified: p.updatedAt ? new Date(p.updatedAt) : new Date(),
        priority: 0.7,
        alternates: { languages: { 'zh-CN': zhUrl, 'en-US': enUrl } },
      });
    });
  });

  const { data: news } = await getNews().catch(() => ({ data: [] as never[] }));
  news.forEach((n) => {
    const zhUrl = `${baseUrl}/news/${n.slug}`;
    const enUrl = `${baseUrl}/en-US/news/${n.slug}`;
    [zhUrl, enUrl].forEach((url) => {
      entries.push({
        url,
        lastModified: n.publishedAt ? new Date(n.publishedAt) : new Date(),
        priority: 0.6,
        alternates: { languages: { 'zh-CN': zhUrl, 'en-US': enUrl } },
      });
    });
  });

  return entries;
}
```

- [ ] **步骤 5：修改 llms.txt/route.ts 输出双语**

修改 `frontend-next/app/llms.txt/route.ts`：

```typescript
import { getSiteSettings, getProducts } from '@/lib/api';

export const revalidate = 3600;

export async function GET() {
  const baseUrl = process.env.NEXT_PUBLIC_SITE_URL || 'http://localhost:3000';

  const [settingsRes, productsRes] = await Promise.all([
    getSiteSettings().catch(() => ({ data: [] as never[] })),
    getProducts().catch(() => ({ data: [] as never[] })),
  ]);

  const settings = Array.isArray(settingsRes.data) ? settingsRes.data[0] : settingsRes.data;
  const products = productsRes.data;

  const content = `# ${settings?.name || '佑森小课堂'} (Yousen Education)

> ${settings?.slogan || '专注幼小衔接教育8年'} | Focus on preschool-primary transition education for 8 years

## 中文版 / Chinese Version
- 首页: ${baseUrl}
- 师资团队: ${baseUrl}/teachers
- 校区环境: ${baseUrl}/campuses
${products.map((p) => `- ${p.name}: ${baseUrl}/courses/${p.slug}`).join('\n')}
- 常见问题: ${baseUrl}/faq
- 退费政策: ${baseUrl}/refund-policy

## English Version
- Home: ${baseUrl}/en-US
- Teachers: ${baseUrl}/en-US/teachers
- Campuses: ${baseUrl}/en-US/campuses
${products.map((p) => `- ${p.name}: ${baseUrl}/en-US/courses/${p.slug}`).join('\n')}
- FAQ: ${baseUrl}/en-US/faq
- Refund Policy: ${baseUrl}/en-US/refund-policy

## Contact
${settings?.phone ? `- 电话/Phone: ${settings.phone}` : ''}
${settings?.email ? `- 邮箱/Email: ${settings.email}` : ''}
${settings?.address ? `- 地址/Address: ${settings.address}` : ''}
`;

  return new Response(content, {
    headers: {
      'Content-Type': 'text/plain; charset=utf-8',
      'Cache-Control': 's-maxage=3600, stale-while-revalidate=86400',
    },
  });
}
```

- [ ] **步骤 6：运行测试验证通过**

运行：`cd frontend-next && npx jest lib/__tests__/seo.test.ts`
预期：PASS（所有用例包括新增 3 个）

- [ ] **步骤 7：Commit**

```bash
cd frontend-next
git add lib/seo.ts lib/__tests__/seo.test.ts app/sitemap.ts app/llms.txt/route.ts
git commit -m "feat(frontend): SEO hreflang + sitemap 双 URL + llms.txt 双语

buildMetadata: 接受 i18n 参数注入 alternates.languages；
sitemap.ts: 每个页面输出 zh-CN + en-US 双 URL + hreflang；
llms.txt: 输出中英文双语段落，课程列表双 URL。"
```

---

## 任务 12：Strapi seed 扩展 en-US 内容

**文件：**
- 创建：`backend/scripts/seed-i18n.ts`

- [ ] **步骤 1：创建 seed-i18n.ts 脚本**

创建 `backend/scripts/seed-i18n.ts`：

```typescript
/**
 * Seed en-US locale entries for existing content.
 *
 * Creates en-US placeholder versions of all zh-CN products, news, campuses,
 * teachers so the i18n E2E tests have content to verify against.
 *
 * Usage (from backend/):
 *   npx tsx scripts/seed-i18n.ts
 */

const EN_PLACEHOLDERS = {
  product: (zh: any) => ({
    title: `${zh.title} (EN)`,
    description: `English description for ${zh.title}. ${zh.description || ''}`,
    locale: 'en-US',
  }),
  news: (zh: any) => ({
    title: `${zh.title} (EN)`,
    content: `English content for ${zh.title}.`,
    locale: 'en-US',
  }),
  campus: (zh: any) => ({
    name: `${zh.name} (EN)`,
    description: `English description for ${zh.name}.`,
    locale: 'en-US',
  }),
  teacher: (zh: any) => ({
    name: zh.name,
    bio: `English bio for ${zh.name}.`,
    locale: 'en-US',
  }),
};

async function seedI18n(strapi: any) {
  const stats = { products: 0, news: 0, campuses: 0, teachers: 0 };

  // Products
  const products = await strapi.documents('api::product.product').findMany({
    locale: 'zh-CN',
    limit: 100,
  });
  for (const p of products) {
    await strapi.documents('api::product.product').create({
      data: EN_PLACEHOLDERS.product(p),
      locale: 'en-US',
    });
    stats.products++;
  }

  // News
  const news = await strapi.documents('api::news-article.news-article').findMany({
    locale: 'zh-CN',
    limit: 100,
  });
  for (const n of news) {
    await strapi.documents('api::news-article.news-article').create({
      data: EN_PLACEHOLDERS.news(n),
      locale: 'en-US',
    });
    stats.news++;
  }

  // Campuses
  const campuses = await strapi.documents('api::campus.campus').findMany({
    locale: 'zh-CN',
    limit: 100,
  });
  for (const c of campuses) {
    await strapi.documents('api::campus.campus').create({
      data: EN_PLACEHOLDERS.campus(c),
      locale: 'en-US',
    });
    stats.campuses++;
  }

  // Teachers
  const teachers = await strapi.documents('api::teacher.teacher').findMany({
    locale: 'zh-CN',
    limit: 100,
  });
  for (const t of teachers) {
    await strapi.documents('api::teacher.teacher').create({
      data: EN_PLACEHOLDERS.teacher(t),
      locale: 'en-US',
    });
    stats.teachers++;
  }

  console.log('[seed-i18n] Done:', stats);
  return stats;
}

async function main() {
  const { createStrapi } = await import('@strapi/strapi');
  const strapi = await createStrapi().load();
  try {
    await seedI18n(strapi);
  } finally {
    await strapi.destroy();
  }
}

if (require.main === module) {
  main().catch((err) => {
    console.error(err);
    process.exit(1);
  });
}

export { seedI18n };
```

- [ ] **步骤 2：Commit**

```bash
cd backend
git add scripts/seed-i18n.ts
git commit -m "feat(backend): seed-i18n 脚本为现有内容创建 en-US 占位版本

为 products/news/campuses/teachers 创建 en-US 占位英文条目，
用于 i18n E2E 测试 fixture。"
```

---

## 任务 13：E2E 测试

**文件：**
- 创建：`frontend-next/e2e/i18n.spec.ts`
- 创建：`frontend-next/e2e/i18n-seo.spec.ts`
- 创建：`frontend-next/e2e/i18n-chat.spec.ts`
- 创建：`frontend-next/e2e/visual-i18n.spec.ts`

- [ ] **步骤 1：创建 i18n.spec.ts**

创建 `frontend-next/e2e/i18n.spec.ts`：

```typescript
import { test, expect } from '@playwright/test';

test.describe('i18n routing', () => {
  test('default locale zh-CN has no URL prefix', async ({ page }) => {
    await page.goto('/courses');
    await expect(page.locator('html')).toHaveAttribute('lang', 'zh-CN');
    expect(page.url()).not.toMatch(/\/en-US\//);
  });

  test('en-US locale has /en-US/ prefix', async ({ page }) => {
    await page.goto('/en-US/courses');
    await expect(page.locator('html')).toHaveAttribute('lang', 'en-US');
  });

  test('LanguageSwitcher switches zh-CN to en-US', async ({ page }) => {
    await page.goto('/courses');
    await page.getByLabelText('切换语言').click();
    await page.getByRole('button', { name: 'English' }).click();
    await expect(page).toHaveURL(/\/en-US\/courses/);
  });

  test('LanguageSwitcher switches en-US back to zh-CN', async ({ page }) => {
    await page.goto('/en-US/courses');
    await page.getByLabelText('切换语言').click();
    await page.getByRole('button', { name: '中文' }).click();
    await expect(page).toHaveURL(/\/courses$/);
  });

  test('locale persists across page reload (cookie)', async ({ page }) => {
    await page.goto('/courses');
    await page.getByLabelText('切换语言').click();
    await page.getByRole('button', { name: 'English' }).click();
    await expect(page).toHaveURL(/\/en-US\/courses/);
    await page.reload();
    await expect(page).toHaveURL(/\/en-US\/courses/);
  });

  test('en-US unknown slug shows 404 in English', async ({ page }) => {
    await page.goto('/en-US/courses/nonexistent-slug-12345');
    await expect(page.locator('html')).toHaveAttribute('lang', 'en-US');
    await expect(page.getByText(/not found|404/i)).toBeVisible();
  });

  test('en-US page with content shows no fallback banner', async ({ page }) => {
    await page.goto('/en-US/courses');
    await expect(page.getByText(/此内容暂无英文版|not available in English/i)).not.toBeVisible();
  });
});
```

- [ ] **步骤 2：创建 i18n-seo.spec.ts**

创建 `frontend-next/e2e/i18n-seo.spec.ts`：

```typescript
import { test, expect } from '@playwright/test';

test.describe('i18n SEO hreflang', () => {
  test('zh-CN page has hreflang link to en-US', async ({ page }) => {
    await page.goto('/courses');
    const enLink = page.locator('link[rel="alternate"][hreflang="en-US"]');
    await expect(enLink).toHaveAttribute('href', /\/en-US\/courses$/);
  });

  test('en-US page has hreflang link to zh-CN', async ({ page }) => {
    await page.goto('/en-US/courses');
    const zhLink = page.locator('link[rel="alternate"][hreflang="zh-CN"]');
    await expect(zhLink).toHaveAttribute('href', /\/courses$/);
  });

  test('sitemap.xml contains both zh-CN and en-US URLs', async ({ page }) => {
    const response = await page.request.get('/sitemap.xml');
    const xml = await response.text();
    expect(xml).toContain('/courses');
    expect(xml).toContain('/en-US/courses');
  });

  test('llms.txt contains bilingual content', async ({ page }) => {
    const response = await page.request.get('/llms.txt');
    const text = await response.text();
    expect(text).toContain('Yousen Education');
    expect(text).toContain('/en-US/');
  });
});
```

- [ ] **步骤 3：创建 i18n-chat.spec.ts**

创建 `frontend-next/e2e/i18n-chat.spec.ts`：

```typescript
import { test, expect } from '@playwright/test';

test.describe('i18n AI customer service', () => {
  test('FloatingChat on en-US page responds in English', async ({ page }) => {
    // Mock DashScope to return English response
    await page.route('**/api/chat/message', async (route) => {
      await route.fulfill({
        status: 200,
        contentType: 'application/json',
        body: JSON.stringify({
          type: 'answer',
          content: 'Here is your answer in English.',
          retrievedDocs: 1,
          isRelevant: true,
        }),
      });
    });

    await page.goto('/en-US');
    await page.getByRole('button', { name: /在线咨询|consult/i }).click();
    await page.getByPlaceholder(/type your question/i).fill('What courses do you have?');
    await page.getByPlaceholder(/type your question/i).press('Enter');

    await expect(page.getByText('Here is your answer in English.')).toBeVisible({ timeout: 10000 });
  });

  test('FloatingChat on zh-CN page responds in Chinese', async ({ page }) => {
    await page.route('**/api/chat/message', async (route) => {
      await route.fulfill({
        status: 200,
        contentType: 'application/json',
        body: JSON.stringify({
          type: 'answer',
          content: '这是中文回复。',
          retrievedDocs: 1,
          isRelevant: true,
        }),
      });
    });

    await page.goto('/');
    await page.getByRole('button', { name: /在线咨询|consult/i }).click();
    await page.getByPlaceholder(/请输入您的问题/i).fill('有哪些课程？');
    await page.getByPlaceholder(/请输入您的问题/i).press('Enter');

    await expect(page.getByText('这是中文回复。')).toBeVisible({ timeout: 10000 });
  });
});
```

- [ ] **步骤 4：创建 visual-i18n.spec.ts**

创建 `frontend-next/e2e/visual-i18n.spec.ts`：

```typescript
import { test, expect } from '@playwright/test';

test.describe('i18n visual regression', () => {
  test('en-US desktop homepage', async ({ page }) => {
    await page.setViewportSize({ width: 1440, height: 900 });
    await page.goto('/en-US');
    await expect(page).toHaveScreenshot('en-US-homepage-desktop.png', {
      maxDiffPixelRatio: 0.01,
      animations: 'disabled',
      mask: [
        page.locator('button[aria-label="在线咨询"]'),
        page.locator('[data-testid="social-links"]'),
      ],
    });
  });

  test('en-US mobile homepage', async ({ page }) => {
    await page.setViewportSize({ width: 375, height: 812 });
    await page.goto('/en-US');
    await expect(page).toHaveScreenshot('en-US-homepage-mobile.png', {
      maxDiffPixelRatio: 0.01,
      animations: 'disabled',
      mask: [
        page.locator('button[aria-label="在线咨询"]'),
        page.locator('[data-testid="social-links"]'),
      ],
    });
  });

  test('en-US courses page desktop', async ({ page }) => {
    await page.setViewportSize({ width: 1440, height: 900 });
    await page.goto('/en-US/courses');
    await expect(page).toHaveScreenshot('en-US-courses-desktop.png', {
      maxDiffPixelRatio: 0.01,
      animations: 'disabled',
      mask: [
        page.locator('button[aria-label="在线咨询"]'),
        page.locator('[data-testid="social-links"]'),
      ],
    });
  });

  test('LanguageSwitcher expanded state', async ({ page }) => {
    await page.setViewportSize({ width: 1440, height: 900 });
    await page.goto('/');
    await page.getByLabelText('切换语言').click();
    await expect(page).toHaveScreenshot('language-switcher-expanded.png', {
      maxDiffPixelRatio: 0.01,
      animations: 'disabled',
    });
  });
});
```

- [ ] **步骤 5：运行 E2E 测试验证通过**

运行：`cd frontend-next && npx playwright test e2e/i18n.spec.ts e2e/i18n-seo.spec.ts e2e/i18n-chat.spec.ts e2e/visual-i18n.spec.ts`
预期：所有用例通过（视觉回归首次运行会生成基线截图）

- [ ] **步骤 6：Commit**

```bash
cd frontend-next
git add e2e/i18n.spec.ts e2e/i18n-seo.spec.ts e2e/i18n-chat.spec.ts e2e/visual-i18n.spec.ts
git commit -m "test(frontend): i18n E2E + 视觉回归测试

i18n.spec.ts: 路由/切换/Cookie 持久化/404/fallback banner；
i18n-seo.spec.ts: hreflang link/sitemap/llms.txt；
i18n-chat.spec.ts: FloatingChat 双语回复（mock DashScope）；
visual-i18n.spec.ts: 4 个基线（en-US 桌面/移动/courses/switcher）。"
```

---

## 自检

### 规格覆盖度

| 规格章节 | 实现任务 |
|---------|---------|
| 2.1 目录结构改造 | 任务 7（next-intl 基础）+ 任务 8（目录迁移） |
| 2.2 next-intl 配置 | 任务 7 |
| 2.3 Strapi i18n 8 个内容类型 | 任务 9（lib/api.ts locale 参数）+ 任务 12（seed） |
| 3.1 i18n 基础设施文件 | 任务 7 |
| 3.2 LanguageSwitcher | 任务 10 |
| 3.3 Navigation/Footer 改造 | 任务 10 |
| 3.4 FloatingChat 双语 | 任务 4（后端）+ 任务 10（前端） |
| 3.5 翻译辅助 API | 任务 6 |
| 3.6 Strapi Admin 按钮（延后） | 不实现（规格声明延后） |
| 4.1 请求→locale 检测→渲染 | 任务 7（middleware）+ 任务 8（[locale] layout） |
| 4.2 Strapi 内容 locale 获取 | 任务 9 |
| 4.3 hreflang/sitemap | 任务 11 |
| 4.4 AI 翻译辅助数据流 | 任务 6 |
| 4.5 AI 客服双语数据流 | 任务 3 + 任务 4 |
| 4.6 知识库 locale 字段同步 | 任务 5 |
| 5.1 locale 检测错误处理 | 任务 7（middleware 内置） |
| 5.2 Strapi 内容缺失 fallback | 任务 9 |
| 5.3 翻译 API 错误处理 | 任务 6 |
| 5.4 AI 客服双语错误处理 | 任务 4 |
| 5.5 messages JSON 缺失 key | 任务 7（onError 降级） |
| 5.6 知识库迁移期错误 | 任务 1 |
| 5.7 Sentry 上报约定 | 任务 6（翻译 API 5xx 上报） |
| 6.1-6.9 测试策略 | 任务 1-13 全部含测试 |
| 7. 实现顺序 | 任务 1-13 按规格顺序排列 |
| 8. 风险与边界 | 任务内已处理（generateStaticParams、Sentry） |
| 9. 验收标准 | 任务 13 E2E 覆盖 |

**遗漏：** 无。所有规格章节均有对应任务。

### 占位符扫描

- 搜索 "TODO"、"待定"、"FIXME"、"后续实现" → 无
- 搜索 "类似任务 N" → 无
- 搜索 "添加适当的错误处理" → 无，所有错误处理都有具体代码
- 所有代码步骤都包含完整代码块

### 类型一致性

- `Locale` 类型在 `i18n/routing.ts`、`lib/api.ts`、`lib/chat.ts`、`FloatingChat.tsx`、`LanguageSwitcher.tsx` 中统一为 `'zh-CN' | 'en-US'`
- `retrieve` 函数签名在 rag-service.ts 和 chat controller 中一致：`(query, topK, locale?)`
- `RetrievalResult` 接口含 `usedFallback` 字段，在 rag-service 和 chat controller 中一致使用
- `buildMetadata` 第三参数 `i18n` 在 seo.ts 和所有页面中一致使用
- `syncSingleContent` 的 `sourceUrl` 格式 `strapi://<uid>/<docId>?locale=<locale>` 在 sync 和 delete 中一致

---

## 执行交接

计划已完成并保存到 `docs/superpowers/plans/2026-07-14-i18n.md`。两种执行方式：

**1. 子代理驱动（推荐）** - 每个任务调度一个新的子代理，任务间进行两阶段审查（规格合规 + 代码质量），快速迭代

**2. 内联执行** - 在当前会话中使用 executing-plans 执行任务，批量执行并设有检查点

**选哪种方式？**
